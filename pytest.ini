[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = -q
markers =
    unit: изолированные тесты чистой логики (без БД/сети/докера)
    smoke: быстрые проверки импорта и сборки
    integration: с реальной временной БД и фейковым awg-слоем
    e2e: сквозные бизнес-сценарии
