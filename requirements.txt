# Проверено актуальным 14.09.2026: все прямые зависимости стоят на свежих
# стабильных версиях. Верхние границы — по мажору: обновление бота подтягивает
# зависимости (pip --upgrade), и без границ однажды приехал бы несовместимый
# мажор ровно тогда, когда его никто не ждёт.
# Требуется Python 3.12+ (используется enum.StrEnum, появился в 3.11; сборка
# ведётся и тестируется на 3.12 — не полагайтесь на голый минимум 3.11).
aiogram>=3.29,<4          # Telegram Bot API framework (стабильная ветка 3.x)
APScheduler>=3.11,<4      # планировщик; 4.x — пред-релиз, в прод не берём
watchdog>=4,<7            # inotify-вотчдог за файлами Amnezia
python-dateutil>=2.9,<3   # relativedelta для календарных месяца/года
tzdata>=2024.1            # таймзоны для zoneinfo; версия календарная, границы не нужны
qrcode>=7.4,<9            # генерация QR-кодов (QR для импорта в AmneziaVPN)
Pillow>=10,<13            # сборка кадров QR в анимированный GIF
PyYAML>=6,<7              # чтение conf/*.yaml (конфигурация приложения)
ruamel.yaml>=0.18,<0.20   # round-trip запись conf/*.yaml из чат-настроек с
                          # сохранением комментариев/порядка (PyYAML их теряет);
                          # чистый Python, ~1.3 МБ, +3.4 МБ RSS, ~11 мс на запись;
                          # 0.x ломает совместимость в минорах — граница по минору
PyNaCl>=1.5,<2            # libsodium: SecretBox + argon2id (шифрование бэкапов,
                          # секреты); restore_backup.py расшифровывает бэкап этим же пакетом.
