# Проверено актуальным на июль 2026.
# Требуется Python 3.12+ (используется enum.StrEnum, появился в 3.11; сборка
# ведётся и тестируется на 3.12 — не полагайтесь на голый минимум 3.11).
aiogram>=3.29,<4          # Telegram Bot API framework (стабильная ветка 3.x)
APScheduler>=3.11,<4      # планировщик; 4.x — пред-релиз, в прод не берём
watchdog>=4               # inotify-вотчдог за файлами Amnezia
python-dateutil>=2.9      # relativedelta для календарных месяца/года
tzdata>=2024.1            # таймзоны для zoneinfo на минимальных системах
qrcode>=7.4                # генерация QR-кодов (QR для импорта в AmneziaVPN)
Pillow>=10               # сборка кадров QR в анимированный GIF
PyYAML>=6                # чтение conf/*.yaml (конфигурация приложения)
PyNaCl>=1.5              # libsodium: SecretBox + argon2id (шифрование бэкапов,
                         # секреты); restore_backup.py расшифровывает бэкап этим же пакетом.
