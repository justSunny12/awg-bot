# awg-bot — журнал изменений

Первые строки каждой записи — адресаты с минимумом, по строке на роль:
`#requires_main_X.Y.Z` (основной бот), `#requires_gw_X.Y.Z` (агент шлюза).
Есть строка — релиз роли адресован; версия — с какой на него прыгают напрямую,
хост ниже сначала обновляется на неё (минимальная поддерживаемая — v2.10.0,
docs/README-bot.md §9a). Строка ставится ТОЛЬКО той роли, чей код релиз
меняет: правка только основного бота — одна строка `main`, только агента —
одна `gw`; обе — когда задет общий код (обновления, бот-каркас, установщик,
общие тексты). Бот получает только свои обновления. Минимум в строке —
версия, которая этой роли была адресована (не каждый патч едет в каждую
роль); 2.10.0 был общим, с v2.20.0 (схема БД: гости, держатели, имена
аккаунтов) минимум основного бота — 2.20.0, агента — по-прежнему 2.10.0. До
v2.18.0 адресат писался хэштегом без минимума:
`#main_bot` / `#gw_bot` / `#all_bots`. Если правка трогает обвязку шлюза —
отдельная строка про пересборку конфигурации шлюза. Строка `#awg_genN`
объявляет поколение AmneziaWG в поставке: боты по нему решают, нужен ли
переезд профилей и можно ли обновляться прямо сейчас (§4a).

## v2.24.6 — файл первого применения везёт поставку агента: шлюзу GitHub не нужен

#requires_main_2.24.0
#requires_gw_2.10.0
#awg_gen1

Основной бот собирает файл; агент — новая первая панель и установщик, поэтому обе роли.
- Шлюз стоит в России, где GitHub без туннеля недоступен, а туннель как раз и ставится файлом первого применения. Инструкция же вела на `curl … awg-bot-install.sh | sudo bash`, и установщик вставал на «качаю поставку» навсегда — на чистой машине агента поставить было нельзя. Теперь основной бот вшивает в файл поставку из СВОЕЙ установки (та же версия, ~1,5 МБ base64 между маркерами после `exit`), а у файла появился режим `--install`: распаковать её во временный каталог и передать управление установщику из неё, тот применит файл как обычно. Команда со своей машины: `scp awg-gw-bundle.sh root@ШЛЮЗ:/root/ && ssh -t root@ШЛЮЗ 'sudo sh /root/awg-gw-bundle.sh --install'`. В интернет с шлюза ходят только apt и pip. Шифрованный файл для чата поставку не везёт — там агент уже стоит; бандл из `awg-bot gw-bundle` тоже, и `--install` на нём честно откажет.
- Greenfield-установка больше не удаляет `install/awg-bot-install.sh` из `/opt`: из этой поставки бот и собирает файл для шлюза, без установщика она была бы неполной. Обновление и так кладёт его.
- Расхождение в конце установки агента: опознание кодом обещало «когда бот запустится, он напишет сюда сам», а установщик заканчивал «напиши /start боту шлюза», и агент не писал. Теперь на первом старте агент присылает панель сам, если диалог есть (после кода — есть), а последняя строка установщика зависит от пути: из файла первого применения — «напиши /start, первым он написать не может», после кода — «бот шлюза напишет сам».

## v2.24.5 — отбой без алерта: состояние монитора меняет доставка, а не отправка

#requires_main_2.24.0
#requires_gw_2.10.0
#awg_gen1

- Монитор шлюза помечал алерт «показан» в тот момент, когда только СОБИРАЛСЯ его отправить. Связь агента с Telegram идёт тем же домом, что и линк, и падает вместе с ним: алерт терялся при отправке, отметка оставалась, а когда всё поднималось, админ получал «✅ Линк до ВПС ожил, хендшейк свежий» первым и единственным словом о происшествии. Теперь отметку ставит рассылка по факту доставки; не дошло — следующий тик скажет то же самое, а если к тому времени починилось, отбоя не будет вовсе. Так же и обратно: не доставленный отбой повторяется, пока не дойдёт.
- Доставкой считается и запасной канал: критичный алерт, уехавший админу письмом, состояние меняет и каждый тик не повторяется.

## v2.24.4 — присланные секреты не остаются в чате

#requires_main_2.24.0
#requires_gw_2.10.0
#awg_gen1

- Агент шлюза принимал пересланную конфигурацию в память, а само сообщение с файлом оставалось в переписке навсегда — вместе с приватным ключом линка, токеном агента, фразой шифрования копий и паролем почты. Свою копию основной бот убирает по «В меню», пересланную не убирал никто. Теперь агент удаляет сообщение сразу, как только узнал свой файл. Чужой файл не трогаем: он не наш секрет.
- Присланная в чат резервная копия — там вся база — убирается так же, у обеих ролей.

## v2.24.3 — агент шлюза стартует после обвязки и говорит, почему таблицы нет

#requires_main_2.24.0
#requires_gw_2.10.0
#awg_gen1

- Таблица `awg_gw_guard` живёт только в ядре: после ребута её восстанавливает юнит обвязки, а с v2.24.2 он ещё и ждёт аплинк. Агент стартовал одновременно с ним, делал проверки раньше и первым сообщением после КАЖДОЙ перезагрузки говорил «таблицы awg_gw_guard нет — шлюз открыт клиентам туннеля», хотя таблица появлялась секундой позже. Юнит агента на шлюзе объявлен `After`/`Wants=awg-link-gw.service`; `Requires` не ставим — не отработавшая обвязка не должна уносить агента, через него её и чинят.
- Замечание об отсутствующей таблице разделено по причинам, они лечатся по-разному: юнит обвязки ещё выставляет её (молчим), юнит не отработал (смотри журнал), юнит не запущен (включи его), юнит жив и таблицы нет (обвязка старого образца — перевыпусти конфигурацию шлюза).

## v2.24.2 — шлюз узнаёт себя по конфигу аплинка, а не только по поднятому интерфейсу

#requires_main_2.24.0
#requires_gw_2.10.0
#awg_gen1

Основной бот везёт исправленный скрипт обвязки в бандле; агент — новые тексты статуса. Чтобы исправление доехало до шлюза, перевыпусти конфигурацию его слота и примени её на той стороне.
- После ребута малины юнит обвязки стартовал раньше `awg-quick@` аплинка. Скрипт искал ключ помеченного устройства только среди ПОДНЯТЫХ интерфейсов, не находил и решал, что слот помечен другой машине: опускал линк и выключал сам себя — шлюз не возвращался после перезагрузки и без рук не чинился. Теперь ключ ищется и в конфигах ещё не поднятых интерфейсов, а юнит объявлен `After`/`Wants=awg-quick@<аплинк>.service`.
- Линки до ВПС (свой слот и слот соседа, `awglink*`) аплинком больше не считаются: их ключ принадлежит паре ВПС↔шлюз, а не устройству, и сравнивать с ним пометку слота нельзя.
- Юнит обвязки себя не выключает: он идемпотентен и перепроверяет пометку при каждом старте. Раньше выключение требовало ручного вмешательства даже там, где машина шлюзом осталась.
- Новое состояние `unconfirmed`: аплинка не видно ни живым, ни в конфигах — сказать «слот помечен другому устройству» не по чему, и линк не трогаем (раньше эта ветка сливалась с чужим шлюзом). Панель агента и отчёт применения говорят об этом отдельно.
- Тексты скрипта и агента — по слотам: «шлюз этого слота — другое устройство» вместо «два шлюза вместе не работают» из времён единственного шлюза.

## v2.24.1 — маскарад в аплинк на шлюзе: агент на чистой машине достаёт до Telegram

#requires_main_2.24.0
#requires_gw_2.10.0
#awg_gen1

Основной бот везёт исправленный скрипт обвязки в бандле; агент — новую проверку.
- Локально рождённый пакет агента выбирает исходный адрес до метки в output: берёт адрес домашнего интерфейса, а после перемаршрутизации по метке улетает в аплинк с чужим src, и ВПС его отбрасывает (у пира разрешён только адрес аплинка). На прежней малине это лечило чужое правило домашней схемы, чистая установка шлюза без него получала агента, который не может достучаться до Telegram. Теперь `routing-gw-setup.sh` ставит `oifname <аплинк> masquerade` в свою nft-таблицу сам, когда шлюз помечен. Чтобы правило появилось на действующем шлюзе, перевыпусти конфигурацию шлюза и примени её на той стороне.
- Монитор здоровья агента: проверка «маскарад в аплинк» по своей таблице — без правила говорит, что делать.

## v2.24.0.4 — карточка устройства-шлюза: пустая строка перед внешним IP, кружок у роли

#requires_main_2.24.0
#awg_gen1

- В карточке устройства-шлюза внешний IP и пинг отделены пустой строкой от потребления, а строка роли получила кружок состояния, как в строке статуса слота: «🟢 [Активен] — несёт трафик РФ-доступа», «🟢 [Резерв] — трафик идёт через …»; 🔴 при недоступности, ⏳ пока связь проверяется.

## v2.24.0.3 — карточка шлюза: хендшейк отдельной строкой, без подсказок

#requires_main_2.24.0
#awg_gen1

- В карточке шлюза под строкой статуса: линк и порт в той же строке, хендшейк — отдельной; строки «Несёт трафик РФ-доступа…», «Наружу проходит, готов принять трафик» и «Трафик сейчас идёт через …» убраны — они дублировали статус.

## v2.24.0.2 — перезапуск юнита линка применяет смену порта

#requires_main_2.24.0
#awg_gen1

- Юнит `awg-link@` останавливал линк абсолютным путём `/usr/bin/awg-quick`, а тулзы лежат там, куда их положил make: остановка падала, интерфейс оставался жить, и реассерт при перезапуске «видел поднятый линк» и не трогал его — смена `ListenPort` в конфиге до ядра не доезжала. Теперь `ExecStop` идёт через сам скрипт (`--down`, `awg-quick` по PATH), а реассерт переподнимает линк, если порт в ядре разошёлся с конфигом. Шаблон прежней версии бот перезаписывает при первом старте, не дожидаясь ребута.

## v2.24.0.1 — порт линка по живому конфигу ВПС; карточка единственного шлюза без «предпочтительного»

#requires_main_2.24.0
#awg_gen1

- В карточке шлюза галочка «Предпочтительный при холодном старте» показывается только когда шлюзов больше одного: с единственным выбирать не из чего. Строки про предпочтительного в разделе с одним шлюзом и так не было.
- Порт линка слота — по живому конфигу на ВПС: сменил `ListenPort` в `awglink.conf` и перезапустил линк — при старте бота строка слота подхватит порт, файервол хоста перерисуется, а админу придёт напоминание перевыпустить конфигурацию шлюза (Endpoint на той стороне старый). Порт в поставке не хранится: только умолчания `routing.link_ports`.

## v2.24.0 — резервный РФ-шлюз: два слота, автопереключение, предпочтительный, пинг до шлюза и его внешний IP

#requires_main_2.22.0
#awg_gen1

Только основной бот; агент шлюза кода не получает, бандлы действующего шлюза перевыпускать не нужно. Концепт и реализация — docs/gateway-failover.md. Схема БД меняется (таблица слотов вместо флага), поэтому следующие релизы основного бота будут требовать v2.24.0.
- **Слоты шлюзов.** Флаг «устройство — шлюз» заменён таблицей `gateways`: устройство админа, линк на ВПС (интерфейс, порт, /30), галочка предпочтительного, домашние подсети, подпись места. Прежний шлюз переезжает в слот 1 сам при первом старте (предпочтительный, активный, ключи бандла — с суффиксом слота). Один шлюз — право, не обязанность: без второго слота всё работает как прежде.
- **Второй шлюз** — ⚙️ Настройки → Условная маршрутизация → «➕ Резервный шлюз»: из моих устройств или новое устройство. На ВПС поднимается свой линк `awglink2` (порт `routing.link_ports[1]`, по умолчанию 8443, файервол хоста открывает его сам), у второго агента свой бот и свой токен (`GW_BOT_TOKEN_2`), файл первого применения `awg-gw-bundle-awglink2.sh`. Юнит линка — шаблон `awg-link@<интерфейс>`, прежний `awg-link.service` переводится на него при первом старте бота после обновления.
- **Автопереключение.** Скользящее окно последних `routing.failover.window_samples` (10 замеров ≈ 5 мин): доступность ниже `min_availability` (50 %), то есть пятый неуспешный замер, подряд или вразнобой, — и резерв три такта проходит → трафик перекладывается на резерв одной командой сразу, на пятом неуспешном; неудачи старше окна не считаются; письмо «🔁 РФ-шлюз переключён». Стрика «плохих подряд» больше нет: то же окно гасит маркировку без резерва, шлёт письмо админу и рисует статусы «не отвечает N мин», поэтому гашение и переключение всегда в одном такте. Липкость: оживший прежний остаётся в резерве («снова в строю — остаётся в резерве»). Второе автоматическое переключение раньше `routing.failover.min_interval_minutes` (10) не делается — гашение и письмо «второе переключение подряд не делаю». Оба лежат — прежнее гашение с добавкой «резервный тоже не отвечает». Молчащий резерв (всё окно неуспешных) — письмо без звука и «снова отвечает». Тумблер «🔁 Автопереключение» в списке шлюзов; такт зонда, ширина окна, порог доступности и автопереключение — экран «📡 Мониторинг и резервирование» последним пунктом раздела условной маршрутизации. Письмо «переехали на резерв» без звука, критичны только отказы без резерва. Зонд резерва идёт по своей таблице и метке (`100 + слот`, `1 << слот`), но редко и с джиттером (`standby_probe_minutes`, 5 мин ± 40 %): строго периодический коннект наружу с домашнего адреса — сигнатура для ТСПУ; между зондами живость резерва — по свежести хендшейка его линка. Ручное переключение на лежащий шлюз ставит удержание: автомат его не перекладывает обратно, пока шлюз не оживёт.
- **Ручное переключение в обе стороны** — карточка слота → «▶️ Переключить трафик сюда», с подтверждением (у лежащего резерва — «Всё равно переключить»); порогов и интервала не учитывает — для обслуживания дома.
- **Предпочтительный шлюз** — галочка в карточке слота, ⭐ в списке, не более одного (держит БД). Берёт трафик при холодном старте — когда перезагружался ВПС (сдвинулся момент загрузки хоста, либо состояния нет); перезапуск одного бота активного не меняет. Не отвечает на старте → сохранённый, иначе любой отвечающий, иначе он всё равно. Замечания preflight — по слотам, резерв — «резерва сейчас нет».
- **Пинг до шлюза и внешний IP.** В карточках слота и устройства-шлюза: «↗️ Внешний IP» — адрес дома шлюза из эндпоинта пира линка (ВПС видит его с каждым хендшейком, сторонних сервисов нет), и «Пинг с AWG-SRV: 43 мс» — ICMP с ВПС до шлюза по линку, медиана трёх (`43 мс`, `1,2 с`); `iputils-ping` ставится установщиком. Пинг кэшируется на сутки, заполняется лениво при первом открытии экрана, снимается при отвале хоста; кнопка «📡 Пинг» последней перед «Назад» меряет заново.
- **Раздел и карточки.** С одним слотом: «🛰 Шлюз: имя» (карточка) и «➕ Резервный шлюз»; с двумя — «🛰 Шлюзы: 2» → список: статус в инфобоксе («🟢 [Активен]», «🟢 [Резерв]», «⏳ [Резерв], проверка связи», «🔴 …, не отвечает N мин»), на кнопках только имя и ⭐, последнее переключение. Вместо «новая машина» везде «новое устройство», «Заменить устройство», «Снять шлюз». **Назначение всегда полным путём**: и для уже созданного устройства ключи линка слота выпускаются заново, спрашивается токен агента слота и приходят инструкция с открытым файлом первого применения (устройство линка не знает); шифрованный файл — только у «Конфигурация шлюза» и у пересланного токена агента. Файл первого применения удаляет себя после успешного применения; повторный запуск установщика с `--role gateway` на машине с агентом не задаёт вопросов — применяет файл и перезапускает агента. Карточка слота: переключить, предпочтительный, конфигурация шлюза, домашние подсети (ввод, с разбором и предупреждением о совпадении подсетей у двух домов), подпись, заменить машину, убрать шлюз (три текста: резервный / активный при живом резерве / последний), пинг. Карточка устройства-шлюза: роль, пинг, «🛰 Карточка шлюза», «📡 Пинг». `routing.home_subnets` в app.yaml устарел: читается один раз при переносе в слот 1.
- **Прочее.** Домашние подсети ведут в линк своего слота, при совпадении у двух слотов — предпочтительному; `awg-bot gw-bundle --link awglink2`; финал переезда профилей переносит слоты на двойников и выдаёт файлы по слоту; пересланный токен агента находит слот по ключу линка; исключение линков из MASQUERADE в файерволе хоста — по всем конфигам с `Table = off`.

## v2.23.1.1 — шлюз не предлагается под ссылку/QR/файл

#requires_main_2.22.0
#awg_gen1

- Кнопки главного меню админа «Ссылка/QR-код/Файл» и «Выдать конфиг» из карточки профиля ставили устройство-шлюз в список выбора; клик упирался в отказ сервиса (конфиг шлюза едет только внутри конфигурации шлюза). Шлюза в этих списках больше нет, как и в шаге «подключение» гайда; у админа, у которого кроме шлюза устройств нет, ряд «Ссылка/QR-код/Файл» в главном меню не показывается («Мои устройства» и «Доступ к РФ-сервисам» остаются), а «Выдать конфиг» отвечает «У профиля нет устройств». Карточка шлюза со своими кнопками, выпуск конфигурации шлюза и двойник при переезде не затронуты.

## v2.23.1 — агент шлюза: тик вдвое дешевле, один коммит на тик, короткий алерт по домашнему каналу

#requires_gw_2.10.0
#awg_gen1

Только агент шлюза (роль gateway). Тик монитора (каждые 3 мин, 480 в сутки) стоил ≈16 exec на малине — стал ≈8:
- аплинк (автодетект — три `awg show`) и политика «Telegram → аплинк» (два `ip -j`) снимались дважды за тик — в проверках и в самовосстановлении; теперь один раз, самовосстановление берёт снятое (≈ −2 400 exec/сутки);
- `systemctl is-enabled` юнита обвязки — в статический ярус (45 мин, кнопки «Статус»/«Монитор здоровья» сбрасывают) вместо exec на тик (−480);
- слово троттлинга/питания Pi — из sysfs (`/sys/devices/platform/soc/soc:firmware/get_throttled`), `vcgencmd` — запасной путь (−480);
- снимок панели, месячный трафик и стрики — одной транзакцией: один fsync на SD-карту за тик вместо трёх;
- проба выхода наружу ходит к обеим целям разом — при лежащем домашнем канале тик держит поток 3 с, а не 6.
- Алерт «шлюз не выходит наружу» — по своему короткому стрику (`app.gateway.egress_alert_streak`, по умолчанию 2 тика = 6 мин, как у линка), а не по общему на пять тиков: лежащий домашний канал для РФ-доступа равносилен лежащему линку.

## v2.23.0 — крупные модули разложены в пакеты; поведение не менялось

#requires_main_2.20.0
#requires_gw_2.10.0
#awg_gen1

Чисто структурный релиз: ни одна строка логики и ни один текст не изменились — тела функций и методов перенесены дословно (сверено по AST: 314 определений texts, 134 keyboards, 242 services, 129 db, 114 admin — без пропусков и правок). Импортные пути снаружи прежние: `from awgbot.domain.services import …`, `from awgbot.bot import texts, keyboards`, `from awgbot.infra.db import Database`, `awgbot.bot.handlers.admin.router`.

- `domain/services.py` (3 800 строк, ~200 методов одного класса) → пакет `domain/services/`: `Services` собирается из миксинов по областям — `blocks`, `clients`, `devices`, `subscription`, `traffic`, `reconcile`, `firewall`, `gateway_link`, `routing`, `status`; типы, исключения и константы — `types.py`.
- `infra/db.py` (2 100) → `infra/db/`: `schema.py` (DDL, конвертеры строк, миграции — порядок в `init_schema` дословный), `core.py` (соединение, транзакции, key-value), миксины `clients`, `devices`, `traffic`, `history`, `routing`, `nav`, `broadcast`.
- `bot/texts.py` (3 000) и `bot/keyboards.py` (2 000) → пакеты по экранам (`fmt`/`common`, `client`, `admin`, `routing`, `settings`, `updates`, `broadcast`, `gateway`, `migration`); `__init__` реэкспортирует всё явным списком, включая используемые снаружи приватные помощники.
- `bot/handlers/admin.py` (2 000, 77 хендлеров) → пакет роутеров `panel`, `clients`, `devices`, `gateway`, `updates`, `selfops`, `blocks`, `broadcast`, включённых в прежнем порядке; относительный порядок регистрации сохранён и закреплён smoke-сторожем `tests/smoke/test_admin_router_order.py` (17 пар «специфичный раньше общего», каждый message-хендлер обязан быть перечислен).
- Пространство имён `awgbot.domain.services` — только своё: класс, исключения, типы результатов, две константы; модули и енумы, которые прежний файл отдавал побочно, снаружи берутся из своих пакетов.
- Тесты: правки только импортов там, где тест патчит модульное состояние (рассылка — теперь через подмодуль `handlers.admin.broadcast`; `texts.admin._HOSTNAME`), сторож роутеров собирает хендлеры по всей цепочке.

## v2.22.3 — быстрый старт, бэкофф самопроверки маршрутизации, параллельный зонд шлюза

#requires_main_2.20.0
#awg_gen1

- **Старт бота 10–30 с → 2–4 с.** До первого `getUpdates` остаётся только необходимое (детект рестарта, сверка пиров, блокировки, первое устройство админа, итог обновления, восстановление панели); списки маршрутизации (HTTP), реконсиляция маршрутизации, SSH-фильтр, замечания при запуске, «бот перезапущен» и отчёт о восстановлении догоняют фоновой задачей после запуска поллинга. Имя/описания бота выставляются по отпечатку в state — совпал, к Bot API не ходим (было 3–4 запроса на каждый старт). Живость awg в замечаниях берётся из стартового замера, а не меряется второй раз.
- **Самопроверка обвязки маршрутизации:** отрицательный вердикт перепроверяется с бэкоффом 60 → 120 → 240 → 300 с вместо каждой минуты (12–18 exec на тик и на каждый рендер экрана, пока сломано); всё, что чинит сам бот, и детект рестарта сбрасывают кэш, «Доктор» меряет живьём.
- **Зонд шлюза** ходит ко всем целям разом, первый успех — ответ (худший случай 2×4 с вместо 2×N×4). Read-only проверки обвязки (`iptables -C`, `ip route/rule show`, `ipset list/save`) — таймаут 5 с вместо 20.
- Отсрочка «продлить на пару недель»: итог встаёт на место предложения одной правкой.

## v2.22.2 — «шлюз не отвечает на старте» при выключенной функции

#requires_main_2.22.0
#awg_gen1

- Замечание при запуске про шлюз условной маршрутизации приходило и при выключенной в настройках функции: preflight смотрел только на развёрнутость обвязки. Теперь выключенная функция на старте не замеряется и молчит; замер переехал в момент включения — включил в настройках, шлюз лежит или без интернета за ним → сообщение тут же, тем же текстом («… не отвечает при включении»).

## v2.22.1 — уведомление о днях паузы при продлении; накопленное сверх порога не сгорает

#requires_main_2.22.0
#awg_gen1

- **Владельцу при продлении** — второй строкой к «Подписка продлена до …»: «⏸ Дней паузы добавлено: +28, доступно 56.» (влезло частично — «… доступно 56 (максимум)» / «(максимум для ежемесячной подписки)»), «⏸ Дни паузы не добавлены: достигнуто максимальное количество для годовой/ежемесячной подписки (N).», ежемесячная после истечения или с отсрочкой в прошлом периоде — «⏸ Дни паузы за этот период не начислены: … Доступно 4 дня.». Бессрочная, день/неделя — без строки. Админу — та же строка коротко в финишере продления («Дней паузы: +28 → 56»).
- **Порог — только для начисления.** Накопленное сверх порога нового типа (год → месяц с 56 на счету) не срезается: выше порога просто не начисляется, пока счёт не опустится ниже.
- Карточка профиля у админа: «Приостановка: доступно 28/56 дней» (год) / «21/24 дней» (месяц) / «21 день» (день/неделя — максимума не показываем), без срока: дни не сгорают.

## v2.22.0 — счёт дней паузы для ежемесячных подписок, экран «Моя подписка», объёмы в ГБ

#requires_main_2.20.0
#awg_gen1

- **Дни паузы — счёт у профиля, ежемесячные тоже копят.** Годовая: +28 при создании на год и при каждом продлении на год (даже несвоевременном), накопление до 56. Ежемесячная: +2 за каждый своевременно оплаченный месяц (создание и продление, пока подписка не истекла и без отсрочки), накопление до 24; смена типа переносит остаток в пределах максимума нового типа; день/неделя не пополняют, у бессрочной счёт всегда ноль. Вход в паузу списывает резерв со счёта, досрочный выход возвращает неиспользованное. Настройки: «Дней паузы (год)» и новая «Дней паузы (месяц)» (`conf/pause.yaml: monthly_pause_days`). Схема: `client_pause.pause_balance_days`; на первом старте счёт выдаётся действующим профилям разово — годовым остаток прежнего лимита, ежемесячным по числу оплаченных месяцев (все считаются своевременными). **Этот релиз — новый минимум основного бота для следующих: `#requires_main_2.22.0`.**
- **Экран подписки.** Кнопка «⚙️ Управлять подпиской» — только пока есть рычаг (войти в паузу или снять свою), иначе «📝 Моя подписка». Содержимое: тип (ежемесячная/годовая/бессрочная/на день/на неделю), статус (🟢 активна / 🟠 истекает DD.MM HH:MM / 🔴 истекла / ⏳ приостановлена), РФ-доступ, период и остаток срока, счёт дней паузы, лимиты потребления и устройств. Потребление и список устройств здесь больше не дублируются.
- Даты в диалогах правки периода принимаются и без времени (`DD.MM.YYYY` → 00:00:00) и без секунд.
- Объёмы везде только в ГБ: ноль — «0 ГБ», любая ненулевая мелочь — «0.01 ГБ», дальше арифметическое округление до сотых, незначащие нули долой; расход и лимит одной строкой: «8.99 из 50 ГБ», у устройства со своим лимитом — «… (лимит устройства)», без лимита — «8.99 ГБ»; разбивка — «(↑ 10 ГБ | ↓ 175 ГБ)». У карточки шлюза единица дублировалась («11.79 ГБ ГБ») — исправлено. Хвост «приостановлено до …» из строки потребления снят: когда снимется блокировка, говорит строка блокировок.
- Главный экран клиента: строка «Потребление за месяц: 24.1 из 50 ГБ» (раньше — только в «Управлять подпиской») и статус «🟠 истекает DD.MM HH:MM» с момента первого напоминания об окончании. Главный экран гостя: потребление по каждому удерживаемому устройству против его лимита (своего, иначе профиля владельца; без обоих — «без ограничений»), одно — строкой, несколько — списком; срок подписки дарителя гостю не показывается.
- Подсказка выбора адресатов объявления с продлением: пометки ∞ и ⛔ — каждая своей строкой.

## v2.21.0 — объявление с продлением подписки; финишер отмены называет диалог

#requires_main_2.20.0
#awg_gen1

- **«📢 Объявление пользователям» — два режима.** Первый экран — выбор: ✉️ *Простое* (как раньше: владельцам и тем, с кем они поделились устройствами) или 💌 *С продлением подписки* — только владельцам, с плюшкой: перед отправкой подписка каждого адресата продлевается на N дней (1–365), а к тексту объявления у каждого своя шапка с датами. Активной — конец периода + N («Длительность твоей подписки увеличена на 10 дней 🙂 / 12.10.2026 → 22.10.2026»); истёкшей — сегодня + N («К длительности твоей подписки добавлено 10 дней с текущей даты 🙂 / Теперь срок подписки: до …»), статус снова активна, держателям её устройств — «доступ вернулся»; бессрочной — объявление без шапки. Начало и тип периода, периодный трафик, долг отсрочки не трогаются. В выборе адресатов режима с продлением справа от имени — ∞ у бессрочных и ⛔ с датой у истёкших; не активировавшие доступ в этот режим не попадают. Шаг дней между адресатами и текстом; лимит текста — за вычетом шапки. Превью — с шапкой и подвалом «кому и на сколько», отчёт — с продлением; недоставка (заблокировал бота) подарка не отменяет.
- Онлайн-кружки в выборе адресатов сняты: не онлайн — прочитает потом. Имена людей в подсказке, превью и отчёте — с именем аккаунта ссылкой, как везде с v2.20.
- Карточка переданного другу устройства у владельца: добавлен «📊 Лимит потребления» — устройство ест квоту дарителя, а менять её он не мог.
- Reply-«✖️ Отмена» говорит, что именно отменено («Добавление устройства отменено.», «Объявление отменено.»…), а не голое «Отменено.».

## v2.20.2 — «онлайн» в панели и в списке по ссылке расходились; кнопка «статус» не переопрашивала пиров

#requires_main_2.20.0
#awg_gen1

- Счётчик «📶 Устройств онлайн» считался опросчиком на момент опроса (раз в тик, 3 мин), а список по ссылке сравнивал те же хендшейки из БД уже с текущим временем: тот, кто был на связи при опросе, через пару минут «гас» в списке при живой цифре в панели — «онлайн: 5», по ссылке «(2)». Теперь «онлайн» везде (список, карточки, кружки в профилях) считается на момент последнего опроса; опрос старше порога — данные протухли, все честно оффлайн.
- Кнопка «статус» в панели переопрашивает пиров: счётчик и хендшейки свежие, а не с прошлого тика.

## v2.20.1 — ревизия кода v2.17.3…v2.20.0: атомарный переход гостя, стойкий разбор ip rule на шлюзе, имена в кнопках, мёртвые обёртки

#requires_main_2.20.0
#requires_gw_2.10.0
#awg_gen1

Ревью коммитов v2.17.3–v2.20.0 (мёртвый код, настройки, роль гостя, агент шлюза). Дыр в логике не нашлось; исправлено и вычищено:

- **Переход гостя во владельцы — один коммит с активацией.** Гостевой профиль удаляется раньше активации (tg_id занят), и сбой между этими шагами оставил бы человека без профиля, а устройства — у никого. Теперь перенос устройств, списка адресов и активация — одна транзакция; наборы маршрутизации перестраиваются после коммита.
- **Агент шлюза: `ip -j rule` с чужими правилами.** Метка с маской (`0x1/0xff`) или нечитаемая роняла разбор всего списка правил — а с ним проверку и самовосстановление политики «Telegram → аплинк». Разбор терпимый: чужое правило просто не наше.
- **Гость, отказ выдачи конфига.** Под сообщением «не удалось выдать» шёл финишер «☝️ Выше — ссылка», а меню под кнопкой уже было снято. Как у клиента: отказ и главный экран следом.
- **Имена людей в кнопках** («Ноутбук — от Вася», «Телефон — Артём» в списках устройств и РФ-доступа) — теперь имя Telegram-аккаунта, как в текстах, а не профильное.
- Вычищено: обёртки без вызывающих (`friend_devices`, `friend_device_by_id`, `guest_donor`, выборки устройств по tg держателя — держатель теперь профиль, ищут по его id), дублирующее переименование гостя в middleware и при повторном коде (имя аккаунта ведёт `tg_name`), дубль вставки гостевого профиля в миграции, лишний параметр, устаревшие докстринги.

## v2.20.0.2 — ссылки на людей по @username: tg://user у посторонних не работает

#requires_main_2.20.0
#awg_gen1

- Ссылка `tg://user?id=…` отрисовывается только у тех, кто этого человека «видит» (контакты, общие чаты); у постороннего она молча становится текстом — «Получено от Serg» без ссылки. Теперь на профиле хранится и публичный `@username` (middleware по сообщениям, фоновая задача — из `get_chat`), ссылка строится как `https://t.me/username` — её видят все; без юзернейма остаётся `tg://user?id=`. На старте после обновления юзернеймы подтягиваются у всех разово.

## v2.20.0.1 — хотфикс: «Мои устройства» и карточка устройства у клиента падали

#requires_main_2.20.0
#awg_gen1

В v2.20.0 два декоратора колбэков (`Menu(devices)`, `DeviceCB(open)`) оказались над вспомогательными функциями, а не над обработчиками: кнопки «📱 Мои устройства» и открытие карточки у клиента отвечали ошибкой. Исправлено; smoke-тест теперь сторожит, что на кнопку не зарегистрирован помощник (функция с подчёркиванием).

## v2.20.0 — роль «гость» и переданные устройства: держатель, один даритель, переход во владельцы, РФ-доступ по держателю

#requires_main_2.10.0
#awg_gen1

Новая модель передачи устройств друзьям (`docs/guest-role.md`, README §6e). Код только основного бота.

**Модель.** У устройства две стороны: владелец (профиль — слот, квота, подписка, разрешение РФ-доступа) и держатель (`devices.holder_client_id` — кто управляет через бота). Профили двух типов: `owner` и `guest` (`clients.kind`); гость — профиль без своей подписки, заводится при первой активации кода `F…`, имя из Telegram, без устройств не закрывается (список адресов и история при нём). Держать чужие устройства может и обычный клиент — у него они не занимают слот. Один даритель на держателя: код от другого владельца отбивается, код цел. Прежние «друзья» становятся гостями автоматически при обновлении (имя «Друг» до первого сообщения).

**Гость.** Свой главный экран (статус сервера, РФ-доступ при фиче у владельца, подписка владельца со ссылкой на аккаунт, «У тебя N устройств»), «Мои устройства», карточка (потребление с указанием, чей лимит, «Получено от …», подключение, блокировка, удаление), ссылка/QR/файл, помощь. Без устройств — «попроси у друга новый код», без кнопок.

**Коды.** `/start F…` и `/code` у гостя и клиента (раньше код здесь молча терялся): ещё одно устройство от того же владельца — «✅ Устройство «…» от [Вася] успешно добавлено»; клиентский инвайт у гостя — переход во владельцы: **все** переданные устройства переезжают в новый профиль независимо от лимита («3 из 2» честно), даритель получает слоты обратно и одно сообщение, админу — сколько перенесено.

**Владелец.** Переданное устройство в карточке — «Передано [Артём] и управляется им», кнопки только «Имя» и «Удалить» (удаление — с предупреждением, держателю уведомление); в списке — 📲, непринятое приглашение — ⏳; клиент-держатель видит чужие устройства после своих с пометкой «от кого», в счётчике — «(+ 1 от [Вася])». Держатель удаляет переданное с предупреждением, что новое не создаст — владельцу «удалено по его запросу. Теперь у тебя N из M устройств».

**Блокировка — с подтверждением** у клиента и гостя (у админа шагом подтверждения служит выбор «тихо / с уведомлением»).

**РФ-доступ.** Субъект — держатель: свои тумблеры, свой список адресов, свой набор; разрешение — владельца устройства. Выдача разрешения включает и переданные устройства и уведомляет держателей («К устройствам, которые тебе передал [Вася]…» / «…больше недоступна 😔»); отзыв гасит их у держателя, флаги сохраняются. У владельца переданные устройства в разделе — без переключателя, с пометкой «Друзья, которым ты передал устройства, управляют функцией самостоятельно».

**Админ.** Перепривязка переданного устройства к другому профилю снимает держателя и перевыпускает ключи (имя и адрес те же): доступ у прежнего держателя пропадает, ему — уведомление; к самому держателю — просто становится владельцем, ключи те же. Удаление админом — держателю «удалено администратором». Код друга админу не даётся.

**Финишеры создания устройства** — «✅ Устройство «…» создано (для друга).\nПотребление устройства …\nКоличество устройств: m/n».

**Люди в текстах — по имени Telegram-аккаунта**, ссылкой на него («получено от», «передано …», «владелец: …», уведомления): профильное имя задаёт админ и оно про подписку. Имя хранится на профиле (`clients.tg_name`/`tg_name_at`): middleware обновляет его по каждому сообщению человека без запросов к API; молчунов раз в сутки (с джиттером) переспрашивает фоновая задача — только тех, чьё имя старше 7 дней или пусто; на старте бота после обновления имена подтягиваются разово у всех. Экраны API не трогают.

**Схема БД меняется** (тип профиля, держатель, имя аккаунта): следующие релизы основного бота будут ставить `#requires_main_2.20.0` — эту ступень нельзя перепрыгнуть. Агент шлюза этим релизом не затронут, его минимум остаётся 2.10.0.

## v2.19.5.1 — «Скрыть» на сообщении «Не удалось обновить»

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

- «⚠️ Не удалось обновить: …» (сетевой отказ GitHub, битая сумма) — финишер со «Скрыть», панель приходит следом; раньше на нём висела «В меню», а без нажатия человек оставался без меню (оно удаляется перед запуском). Обе роли.

## v2.19.5 — агент чинит политику «Telegram → аплинк», которую сносит systemd-networkd

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

**Дефект (16.09.2026).** Агент шлюза потерял Telegram на пять часов: аплинк жив, метка на адресах Telegram стоит, но пропали правило `fwmark 1 → таблица 100` и маршрут по умолчанию в аплинк в этой таблице — трафик агента ушёл домашнему провайдеру, `getUpdates` висел до таймаута. Правило и маршрут ставит PostUp аплинка при подъёме, а снёс их перезапуск systemd-networkd: по умолчанию он удаляет чужие ip rule и маршруты (`ManageForeignRoutes`/`ManageForeignRoutingPolicyRules=yes`). Обновиться агент тоже не мог — проверка обновлений идёт тем же путём.

- **Агент** (роль gateway): проверка «политика аплинка» в мониторе здоровья и панели; пропавшее правило/маршрут перевыставляются на том же тике двумя idempotent-командами `ip`, без рестарта юнита; одно уведомление «перевыставлена: …» на факт.
- **Скрипт обвязки шлюза** (`routing-gw-setup.sh`, едет в бандле с ВПС): шаг 1b — drop-in `/etc/systemd/networkd.conf.d/awg-gw.conf` (`ManageForeignRoutes=no`, `ManageForeignRoutingPolicyRules=no`) и перевыставление правила/маршрута при каждом применении — реассерт юнита тоже чинит. Откат drop-in убирает. **Чтобы drop-in доехал до шлюза, перевыпусти конфигурацию шлюза с ВПС и примени её на той стороне** — самолечение агента работает и без этого.

## v2.19.4 — общее ядро диалогов настроек для обеих ролей; мастер почты; автобэкап

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

Остаток ревью 15.09.2026 — правки общих модулей, поэтому обеим ролям.

- **Диалоги настроек** (ввод значения, парольная фраза бэкапов в два шага, «бэкап сейчас», канал бэкапа, пять email-действий, тумблер с проверкой «ящик настроен») жили двумя копиями — в основном боте и в агенте — и расходились. Теперь одно ядро (`handlers/settingscore.py`); роль отдаёт только колбэки и клавиатуры. Агент получил то же, что основной бот в v2.19.1–v2.19.1.1: финишер «✅ Частота опроса успешно изменена: 3 → 5 мин.», вопрос удаляется, ввод и переспросы убираются, раздел приходит следом.
- **Мастер подключения почты** (обе роли): после завершения раздел приходит через общий механизм меню — прежний экран гаснет; вопросы мастера и ответы человека убираются, в чате остаётся итог «ящик подключён» (пароль удалялся и раньше).
- **Автобэкап** — одно тело задачи на обе роли; чтение канала и отметка месяца ушли из event loop в поток, как остальная синхронщина.
- Снято из `secrets_util`: неиспользуемый отпечаток секрета и пустой блок от снятой подписи; base64-хелпер, живший только в тестах.

## v2.19.3 — новое устройство разрешённого профиля сразу в режиме РФ-доступа; выключение фичи целиком — с подтверждением

#requires_main_2.10.0
#awg_gen1

- **Новое устройство профиля с разрешённым РФ-доступом приходит с включённым режимом** (решение 16.09.2026; до этого — выключенным, «чтобы не ходить через шлюз молча»). Обещание «включено для всех твоих устройств» теперь держится и для добавленных позже; двойник переезда наследует флаг. Без разрешения — как раньше, выключено.
- **Общий выключатель условной маршрутизации** (Настройки → Маршрутизация): выключение — через подтверждение «⚠️ Выключить условную маршрутизацию для всех?» с «⬅️ Отмена» / «🔴 Да, выключить»; включение — сразу.

## v2.19.2 — выдача РФ-доступа включает все устройства и уведомляет владельца; удаление устройства клиентом; иконка ⏳

#requires_main_2.10.0
#awg_gen1

- **Выдача РФ-доступа профилю** (Настройки → Маршрутизация → Доступность пользователям) теперь включает режим сразу на всех устройствах профиля — раньше после выдачи админу приходилось включать их руками. Отзыв флаги устройств не трогает. Владельцу приходит уведомление: при выдаче — «К твоей подписке добавлена функция РФ-доступа 🎉…», при отзыве — «Функция РФ-доступа больше не входит в твою подписку 😔…»; повторное нажатие того же состояния — молча.
- **Удаление устройства клиентом**: на месте вопроса остаётся «🗑 Устройство «Телефон» удалено. Теперь можно добавить до 2 устройств.», следом — «Мои устройства», а если удалили последнее — сразу главное меню (пустой список с одной кнопкой «Назад» ничего не говорил).
- **Список устройств**: устройство, отданное другу, который ещё не принял инвайт, — ⏳ вместо 📲. Иконка теперь одна на тексты и кнопки.
- После создания устройства на экране «Как планируешь подключить» выход называется «В меню», а не «Назад».
- Уведомление о смене лимита устройств: «Максимальное количество устройств для тебя изменено: 1 → 2.»
- Финишер после ввода в настройках — без кавычек вокруг имени: «✅ Частота опроса успешно изменена: 3 → 5 мин.»

## v2.19.1.1 — финишер после ввода в настройках, тексты списка РФ-адресов

#requires_main_2.10.0
#awg_gen1

- **Ввод значения в «Настройках»**: вопрос «введи новое значение» после ответа удаляется, в чате остаётся финишер «✅ Частота опроса успешно изменена: 3 → 5 мин.» (глагол согласован с именем настройки), раздел с новым значением — следом. Для DNS клиентов показываются оба адреса, для адреса писем — «сам ящик», для SSH-вайтлиста — что добавлено. Вопросы порта переезда и парольной фразы убираются так же.
- **Список РФ-адресов**: добавление — «✅ megafon.ru добавлен в список сайтов, открываемых с российского адреса» (несколько — списком) и подсказка «если сайт всё ещё ругается на VPN или не работает — переподключись»; удаление получило свой итог «🗑 megafon.ru удалён из списка…» на месте панели, панель следом (раньше — только всплывашка).

## v2.19.1 — ревью сценариев: чужой РФ-список у админа, одно живое меню, джиттер проверки обновлений

#requires_main_2.10.0
#awg_gen1

Итог сквозного ревью переходов, мёртвого кода и подходов. Код только основного бота; агент шлюза не задет.

**Исправлено.**
- **Админ правил чужой список РФ-адресов у себя.** Из панели чужого профиля «➕ Добавить адреса», «➖ адрес» и «🗑 Очистить» действовали над списком самого админа: обработчики этих кнопок брали профиль «чей чат», а не из кнопки. Теперь все действия раздела (панель, устройства, тумблеры, список) берут профиль одинаково: клиент — свой, админ — тот, чей id в кнопке (без id — свой); «Назад» ведёт туда, откуда пришли. Дубли админских обработчиков в admin.py сняты. Тесты: правки чужого не трогают свой, клиент не дотягивается до чужого, экран устройств одинаков для обоих и переключения видны обоим, отзыв разрешения закрывает раздел обоим и не стирает настройки.
- **Проверка обновлений основного бота ходила на GitHub ровно в назначенную секунду** — своя копия триггера без джиттера. Теперь одна фабрика на обе роли, ±30 минут.
- **Ввод в «Настройках» оставлял два живых экрана.** Раздел после текстового ввода (значения, порт переезда, вайтлист SSH, пассфраза) уходил обычным сообщением: приглашение с «Отмена» оставалось живым, нав-указатель на нём, ввод и вопросы — в чате навсегда. Теперь раздел приходит через общий механизм меню, прежний экран гаснет, служебное убирается при возврате.
- **Понижение лимита устройств → «Да»**: панель присылалась, а затем с неё снимались кнопки ради «Готово.» с меню на месте вопроса — живое меню оказывалось над мёртвой панелью. Теперь на месте вопроса — «Лимит устройств профиля «имя» изменён: X → Y», панель следом.
- **Блокировка с приостановкой, «Другое» в паузе, «Себе/Другу» при добавлении устройства**: экран с кнопками не гасился при переходе к вводу, вопросы не убирались. Гасится, убираются; в «для друга» показаны слоты профиля («2 из 3»), как и в «себе».
- **Клиент переименовал устройство — и остался без меню.** Единственный диалог без возврата; теперь как у админа: итог и меню следом.
- **Выход из приостановки (админом и клиентом)**: отчёт приходил под карточкой, карточка переписывалась выше. Теперь итог на месте, экран следом — как у продления и переезда.
- **Токен агента при отказе оставался в чате**; удаляется первым действием, принят он или нет.

**Снято.** Алиас старой кнопки «добавить устройство» у админа и ветка «админ добавил устройство себе через карточку» (карточки админа в списке давно нет); `routing_toggle_for_client` без единого вызова; устаревший ключ `app.routing.probe_target` (список `probe_targets` старше минимальной версии); параметры-призраки `fresh` и `last`; тройной сброс состояния при входе в рассылку.

**Подчищено.** Пороги уведомлений (`notified_thresholds`, `traffic_notified`) читаются из уже загруженного профиля, а не отдельным запросом на каждого клиента в каждом проходе. Пятнадцать одинаковых обработчиков «ссылка / QR / файл» (админ, свои устройства, клиент, устройство, друг) сведены к пяти — по одному на вход; у друга QR идёт общим путём. Карточка устройства после блокировки рисуется тем же кодом, что и при открытии. Тест клавиатуры перенесён из тестов сервисов; неиспользуемый рычаг «сервер не отвечает» в заглушке снят.

## v2.19.0.4 — снятый релиз не глушит обновления у тех, кто его поставил

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

Правило «установленной версии нет среди тегов релизов — нерелизная сборка, обновления выключены» сработало на снятом v2.19.0: основной бот на нём отвечал «версия актуальна» и не видел хотфиксов. Теперь своей считается и версия, у которой есть хотфиксы той же базы X.Y.Z.N — снятый релиз обновляется на них. Чужой номер по-прежнему молчит. Релиз v2.19.0 возвращён на GitHub с пометкой ради тех, кто на нём стоит.

## v2.19.0.3 — обновление чинит резолвер, сломанный v2.19.0

#requires_main_2.10.0
#awg_gen1

Хост, прошедший через v2.19.0, остался с конфигом резолвера, из-за которого dnsmasq в рестарт-цикле; v2.19.0.2 такой конфиг не трогала («уже под опекой»). Теперь `post_update` при живом конфиге и **неактивном** dnsmasq переписывает конфиг заново — правильными ключами. Релиз v2.19.0 снят с GitHub (код остался): его усыновление роняло DNS у клиентов.

## v2.19.0.2 — хотфикс: dnsmasq не стартовал после усыновления резолвера

#requires_main_2.10.0
#awg_gen1

**Дефект.** dnsmasq принимает `bind-interfaces` и `cache-size` **один раз на все файлы конфигурации**, а Ubuntu кладёт `bind-interfaces` в `/etc/dnsmasq.d/ubuntu-fan` (файл дистрибутива без `.conf`, но читается). Мой `awgbot-resolver.conf` нёс `bind-dynamic` (несовместим с `bind-interfaces`) и `cache-size` (дубль с конфигом обвязки) — «illegal repeated keyword» / «cannot set --bind-interfaces and --bind-dynamic», демон в рестарт-цикле, клиенты с приватным DNS без резолва. Монитор доложил сразу.

- `bind-dynamic` снят. `bind-interfaces` и `cache-size` пишутся в наш файл только если их нет в других файлах dnsmasq; из конфига обвязки (наш) однократные ключи и наши адреса убираются.
- Поздний подъём адреса `.1` (после старта dnsmasq) закрывает override юнита: `Restart=on-failure` без лимита попыток.
- **Проверка до рестарта**: `dnsmasq --test`; не прошла — свой конфиг откатывается (прежний возвращается, у первой установки снимается), демон поднимается на прежнем, скрипт падает с текстом ошибки. Тот же откат при упавшем демоне после рестарта: с нашим битым файлом хост не остаётся.
- Лечение на месте, если уже наступили: `sed -i '/^bind-dynamic$/d; /^cache-size=/d' /etc/dnsmasq.d/awgbot-resolver.conf && systemctl restart dnsmasq`.

## v2.19.0.1 — из уведомления об обновлении убрана ссылка на diff кода

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

Строка «Все изменения одним списком» вела на `compare` GitHub — на исходники. Админу нужен changelog, а не код; ссылки на страницы пропущенных релизов остаются.

## v2.19.0 — свой DNS-резолвер клиентов

#requires_main_2.10.0
#awg_gen1

**Что.** dnsmasq на собственном адресе сервера в туннеле (`<подсеть>.1`), в конфигах клиентов оба поля DNS — на него. Публичный адрес в поле DNS — это Chrome/Edge, молча уходящие на DoH мимо любого перехвата `:53`: набор адресов условной маршрутизации наполняет только наш резолвер, и у такого человека РФ-сайты ругались на адрес «через раз». Заодно запросы клиентов не уходят третьей стороне открытым текстом, есть кэш и защита от DoH-обхода и DNS rebinding — с первого дня, а не только со шлюзом (README-bot §4c).

- **Свежая установка** получает резолвер сразу: `install/awg-resolver-setup.sh` (`bind-dynamic` — адрес интерфейса появляется вместе с ним; апстримы, `stop-dns-rebind`, NXDOMAIN на DoH-эндпоинты; override юнита с `Restart=on-failure`), поля `dns1/dns2` записаны установщиком. Не поднялся — предупреждение, поля остаются публичными. CLI `awg-bot resolver status|install|add|remove`.
- **Живая установка с публичным адресом**: инфобокс при каждом старте до решения и кнопка «🔒 Свой DNS-резолвер» в «🖥 Сервер AWG» — что даёт, чего стоит, три ответа: «переехать сейчас» (открывает подготовку переезда), «при следующем переезде» (конфиги и так перевыпускаются), «не нужно». Docker-режим не предлагается.
- **Переезд**: при включении рычага резолвер начинает слушать `<новая подсеть>.1` до рождения двойников (не поднялся — переезд не начинается); DNS двойников — по интерфейсу, как протокол (`app.docker.migration_dns`); финал переписывает `dns1/dns2` и снимает старый адрес, отмена снимает новый. Хост с уже приватным адресом получает новый при любом переезде — старый умирает со старым интерфейсом.
- **Обновление**: хост, где `dns1 = <подсеть>.1` прописан руками, берёт резолвер под опеку бота (`adopt_resolver`); из `awgbot-base.conf` обвязки снимается несовместимый `bind-interfaces`.
- **Обвязка маршрутизации**: при резолвере бота dummy `awgdns0` не поднимается, перехват `:53` старых профилей ведёт на адрес резолвера, в `awgbot-base.conf` — только адрес перехвата. `--rollback` не роняет `awgdns0` и его dnsmasq, пока `10.255.53.1` (или фасад на нём) указан в `dns1/dns2`: адрес вшит в конфиги, снимет его переезд.
- **Здоровье**: тик монитора шлёт UDP-запрос на приватные адреса — алерт со звуком (и на почту) один раз, «снова отвечает» один раз; preflight предупреждает на старте.
- ROADMAP: §8 закрыт, из бэклога сняты ротация ключа линка и резервный путь агента.

## v2.18.1 — шимы совместимости старше минимальной версии сняты

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

Минимальная поддерживаемая версия — v2.10.0 (v2.18.0): всё старше проходит через неё и получает там свои миграции, поэтому код новее держать их не обязан.

- **БД**: сняты доводящие миграции `_migrate_additive` (колонки маршрутизации, переезда, `content_msg_ids`, `resume_code`), `_migrate_samples_last_update`, `_migrate_drop_duplicate_indexes`, `_migrate_drop_greeted`, `_migrate_routing_domains_mode`, `_migrate_routing_master_to_devices`, `_migrate_gateway_flag` — схема v2.10.0 уже полная. Единственный уникальный индекс шлюза переехал в SCHEMA. Осталась `_migrate_drop_full_access` (стирание секрета): её оставляем при любом минимуме.
- **env → БД**: перенос `BACKUP_KEY`/`BACKUP_PASSPHRASE` и `EMAIL_RESUME_LOGIN`/`PASSWORD` из env и напоминания «строки из env можно удалить» сняты вместе с чтением этих переменных; шаблон `.env.example` — только `BOT_TOKEN` и `ADMIN_ID`. Случайный ключ, перенесённый в БД раньше, продолжает действовать до задания фразы.
- **Источники списков маршрутизации**: совместимость с ключом `rt_src_bad:` прежней схемы докладов снята.
- **Расписание проверки обновлений**: снятый вариант `hour` больше не переписывается в yaml — неизвестное значение строится как `day`, конфиг не трогается.
- Оставлен `retire_legacy_ssh_gate`: он снимает цепочку `AWGBOT_SSH` прежних ворот **в момент включения файервола**, а не при обновлении, — установка, прошедшая через v2.10.0 без включённого файервола, всё ещё может нести её.
- Тест `test_fresh_schema_is_complete_and_idempotent` фиксирует, что SCHEMA создаёт всё, что читает код; прыжок с реальных данных v2.10.0 — `test_update_jump.py`.

## v2.18.0 — обновление одним прыжком: последний релиз роли с обязательными ступенями

#requires_main_2.10.0
#requires_gw_2.10.0
#awg_gen1

**Механизм обновлений.** Прежде бот шёл по одной версии вверх, и каждая ступень стоила сборки ядра (минуты компиляции и подмена модуля — секунды простоя туннелей), рестарта, а при смене поколения — отдельного переезда профилей. Промежуточные ступени ничего не добавляли: ядро прибито к поставке, миграции БД идемпотентны, conf досеивается. Теперь цель обновления — **последний релиз, адресованный роли**; всё между ним и установленной версией перепрыгивается.

- **Адресат и минимум — одной строкой на роль** в теле релиза: `#requires_main_X.Y.Z`, `#requires_gw_X.Y.Z`. Есть строка — релиз роли адресован; версия — с какой на него можно прыгнуть напрямую. Хост ниже сначала обновляется на неё (ближайший релиз своей роли не ниже), потом дальше; у ступени может быть своя. Прежние `#main_bot`/`#gw_bot`/`#all_bots` читаются у старых релизов.
- **Минимальная поддерживаемая версия — v2.10.0** (манифест ядра, файл состояния, единый архив). Всё старше проходит через неё и получает там свои миграции; шимы совместимости под версии ниже минимума снимаются в следующем релизе.
- **Уведомление** перечисляет пропущенные версии ссылками на их страницы релизов (changelog не теряется) плюс ссылка на разницу целиком; хвост длиннее десяти сворачивается. Превью ссылок у бота выключены глобально — иначе карточка первой ссылки.
- **Идёт переезд профилей** — цель не выше его поколения: релизы дальше не рассматриваются, берётся последний под потолком; под потолком ничего — экран «Обновления» объясняет, чего ждём.
- **Усыновление поколения исправлено.** Обновление без файла состояния приехало с домантифестной версии — хост первого поколения, а не поколения поставки: прыжок с v2.9 на gen-2 поставку иначе записал бы хост переехавшим, не переезжая. Первую установку файлом снабжает создание сервера.
- Тест прыжка: БД и conf образца v2.10.0 (созданы кодом v2.10.0) открываются текущим кодом, миграции проходят разом, экраны рисуются на дефолтах; апдейтер v2.10.0 находит в текущем архиве `awgbot/__main__.py` и передаёт управление новому скрипту.

## v2.17.3 — ревью кодовой базы: мёртвый код и неактуальные тесты

#all_bots
#awg_gen1

**Два дефекта, найденные ревью.**
- Кнопка «✏️ Задать порт» в подготовке переезда отвечала «Эта настройка недоступна»: общий обработчик ввода настроек стоял в роутере раньше специального и перехватывал нажатие. Обработчик переставлен; тест порядка фильтров теперь сторожит и эту пару.
- На карточке чужого устройства с незавершённым инвайтом админу показывалась «🔁 Перевыдать инвайт», а обработчик у неё только в роутере клиента — нажатие крутилось до таймаута. Кнопка осталась только у владельца: инвайт уходит в его чат.

**Снято.** Обёртка `install/harden_firewall.sh` (единственная точка — `awg-bot firewall`), `tools/manage_secrets.py` и вопрос про шифрование бэкапов в `--advanced` (ключ давно живёт в БД и задаётся с экрана бота; скрипт писал его в env, откуда бот переносил и просил удалить). Остатки снятых сценариев: `release`-токены в подписи шлюза (`gwsign` принимает только `claim`, лишние поля старых агентов не мешают), поле `previous` у `gateway_claim`, `RoutingCB.page`, обработчик `del_menu` без кнопки, `gateway.doctor`, `drop_mss_clamp`, `_wan_if` с ключом `wan_interface`, `container_running`/`restart_container`/`ContainerDown`, `MIGRATION_AVAILABLE` (ещё и неверный), `BACKUP_ENCRYPTION_ENABLED`, `CONF_BAK_PATH`, `DEVICE_ADMIN`/`CLIENT_ADMIN`, `get_float`, `parse_networks`, дубли `_panel_text`, `push_nav_history`, `_count_devices_raw`, `routing_toggle_for_client`; тест-онли методы БД (`add_traffic`, `get/set_sample`, `set_states`, `broadcast_recipients`) — тесты переведены на боевые батч-версии. Хук перевешивания `probe_seconds` больше не регистрируется на несуществующий job при выключенной маршрутизации.

**Комментарии и докстринги** приведены к текущему устройству: `config.py`, `db.py`, `watcher.py`, `awg.py`, `app.yaml`, `restore_backup.py` (один архив, а не пофайловые `.enc` с `docker cp`), докстринги `callbacks.py` перечисляют реальные действия.

**Тесты.** Удалены дубли между слоями и тавтологии; грепы по исходникам заменены прогоном: `pair` и `first_device` целиком, `ensure_awg_generation` и `prune_old_kernel_builds` из `awg-bot.sh` с настоящими `yaml_*`/`awg_state_*`, `install_self` трёх скриптов обвязки, юнит `routing-host-setup.sh` рендером, блоки `awg-host-migrate.sh` (пиры, мультиплексор), `detect_container` и копирование конфига в `routing-gw-setup.sh`, `configure_gateway` на шаблоне. Порт живого интерфейса на экране сервера проверяется по-настоящему (раньше — тавтологией). Файлы-свалки разнесены: объявления — `test_handlers_broadcast.py`, почта и бэкапы из чата — `test_handlers_settings_mail_backup.py`, раскладка настроек и значки — `test_settings_layout.py`. Тесты «хендлер не упал» получили проверку того, что показано. `pyflakes` чист по `awgbot`, `tools` и `tests`.

**Отложено (docs/ROADMAP.md, бэклог):** периодные счётчики трафика — write-only; миграции схемы старых установок — до решения о минимальной поддерживаемой версии.

## v2.17.2 — ядро опознаётся по тегу, а не по строке версии

#all_bots
#awg_gen1

**Найденный дефект.** Апстрим не бампает `version.h`: теги `v3.1.20260812`, `…0827`, `…0828` и `…0906` все рапортуют версию `3.1.20260812`. Наша сверка сравнивала именно эту строку — значит не отличала теги вовсе и молча пропустила бы смену тега в манифесте, ради которой весь механизм и делался. Проверка исходников на боевых хостах показала, что они собраны из `v3.1.20260906`, хотя всё вокруг показывало `3.1.20260812`.

- Манифест хранит **тег** (`AWG_MODULE_TAG`, `AWG_TOOLS_TAG`); версия осталась для показа человеку.
- Установщик записывает собранный тег в состояние хоста и сверяет с манифестом. Записи нет (собирали руками или до появления состояния) — модуль пересобирается: узнать правду иначе неоткуда.
- «Работает ли то, что собрано» проверяется по `srcversion` — хешу исходников. Прежнее сравнение строк версии не срабатывало никогда, потому что строки совпадали всегда.
- Дерево DKMS называется по тегу (`/usr/src/amneziawg-3.1.20260906`): иначе разные теги делили бы один каталог, с тем же исходом, что у апстримного `1.0.0`.
- Раздел «Сервер AWG» показывает тег ядра, а не только строку версии.

**Манифест поднят до `v3.1.20260906`** (тулзы остаются на `v3.1.20260812`, новее у них нет). Что там починили: инициализация блокировки в защите заголовков и `down_write` при смене ключа — это ровно та подсистема, которой мы пользуемся (H1..H4); сборка с ядрами 7.1.5+ и 7.2. Изменения на проводе касаются только выключенных у нас возможностей: хвосты у джанк-пакетов и I1..I5, заполнение по окну UDP, поведение при `DisableCookies`. Поколение прежнее, переезд профилей не нужен.

**Прежние сборки убираются.** Дерево прежнего тега держится ровно до тех пор, пока нужно для отката: при совместимой смене тега — до успешного старта бота на новом модуле (и убирается сразу после него), при смене поколения — до финала переезда профилей (пока старый интерфейс обслуживает людей, путь назад должен оставаться). Сюда же попадает апстримное дерево `1.0.0` с хостов, собранных руками. Режим `awg-bot awg prune` отказывается работать, пока загружен не тот модуль, что собран.

**Зависимости.** Все прямые зависимости на свежих стабильных версиях, кроме данных таймзон (`tzdata` 2026.3 → 2026.4). Обновление бота теперь подтягивает зависимости (`pip --upgrade`): раньше venv на хосте застывал на том, что стояло при первой установке. Чтобы это не привезло несовместимый мажор, у всех зависимостей появились верхние границы.

**Что произойдёт при обновлении.** Хосты, где тег не записан (все существующие), пересоберут модуль из `v3.1.20260906` и подменят работающий: все awg-интерфейсы лягут на секунды. На шлюзе это означает потерю Telegram до подъёма аплинка, отчёт придёт после. После успешного старта прежняя сборка (`amneziawg-1.0.0`) убирается из DKMS и `/usr/src`. Порядок прежний: сначала ВПС, потом шлюз.

## v2.17.1.2 — кружок онлайна из «Моих устройств» убран

#main_bot
#awg_gen1

- Второй значок в списке своих устройств (онлайн) убран: два кружка подряд в каждой строке превращают список в рябь. Тип устройства остаётся, а «кто сейчас в сети» и так есть отдельным экраном.

## v2.17.1.1 — значки состояния и честные счётчики

#main_bot
#awg_gen1

- В списках выбора («Доступность пользователям», «События профилей» в уведомлениях) вместо кружков — галочки, те же, что в списке устройств маршрутизации и в выборе адресатов рассылки. Кружок читался как «профиль жив или лежит», то есть как состояние того, что перечислено, а не как отметка выбора. Сами настройки уведомлений не тронуты.
- Кружок в списке профилей стал означать ОНЛАЙН: зелёный — подключён хотя бы один пир, красный — никого, ⏳ — профиль ещё не активировал доступ. Состояние подписки и так видно в карточке, а «кто сейчас в сети» из списка узнать было негде.
- **Счётчики перестали врать.** Шлюз считается обычным устройством: он есть в счётчике слотов, в счёте онлайна и влияет на число доступных для добавления. Прежде он вычитался из счётчиков, но оставался в списках — «устройств добавлено 5» при шести строках и «онлайн 5» при четырёх. Шлюзом может быть только устройство админа, а его профиль безлимитный, так что прятать строку было не ради чего.
- Трафик шлюза в потреблении профиля по-прежнему не учитывается: через него идёт РФ-трафик всех клиентов, это не расход админа. Лимиты трафика на него тоже не действуют — заблокировать шлюз значит отключить маршрутизацию всем.

## v2.17.1 — вычитка текстов настроек

#all_bots
#awg_gen1

Агенту шлюза эта поставка нужна ради перестановки настроек из v2.17.0: та запись была адресована только основному боту по ошибке, и агент её пропустил. Кода агента правки ниже не касаются.

- Раздел «Сервер AWG»: убраны строка про живой порт и порт линка (она объясняла внутреннее устройство там, где его не спрашивали) и «Кнопка ниже» в конце.
- «Обслуживание»: убрана вводная фраза и хвост про переезд. Про переезд раздел рассказывает теперь ровно тогда, когда его кнопка здесь есть, — без кнопки это разговор о механизме, которого в экране не видно.
- Подтверждение перезапуска AWG больше не обещает восстановить блокировки: без контекста непонятно, о каких блокировках речь, а происходит это само собой.
- Настройки обоих ботов начинаются с уведомлений.

## v2.17.0 — смена порта и подсети из бота, разбор замечаний по настройкам

#main_bot
#awg_gen1

**Адресат указан неверно.** Перестановка настроек касалась и агента шлюза (у него уведомления тоже переехали наверх), а релиз помечен только основным ботом — агент такой релиз фильтрует и пропускает. Исправлено в v2.17.1, она адресована обоим.

**Переезд профилей из интерфейса**
- Раздел «🖥 Сервер AWG» получил кнопку «🚚 Сменить порт или подсеть». Бот поднимает второй интерфейс (свободное имя, свободная подсеть, порт случайный или заданный вручную), пишет ключи переезда в конфиг и перезапускается; дальше работает обычный переезд профилей. Раньше второй интерфейс заводили руками в `app.yaml`, и о самой возможности человек узнавал, только если уже знал.
- Экран перед стартом называет текущую топологию, размер когорты и цену: старые пиры работают до финала, отмена безопасна, на финале непереехавшие теряют коннект.
- **Запрет симметричный.** Идёт переезд по поколению — кнопки нет, и раздел объясняет почему. Идёт переезд по кнопке — поставка с новым поколением не предлагается, а установщик не переписывает цель переезда: иначе на финале хост записался бы перешедшим на ядро, которого не видел. После финала бот сам предлагает подготовить следующий переезд, если смена поколения ждёт очереди.

**Настройки**
- Уведомления первым пунктом в обоих ботах.
- Инфобокс «Обслуживания» рассказывает про мониторинг и резервные копии, которые туда переехали.
- «Параметры подписок» объясняют, что такое бонус-квота, максимум дней паузы и грейс-период.
- Раздел доступа по SSH получил свой заголовок; обещание про таймер отката осталось только там, где фильтр ещё выключен; строка про NAT убрана.
- В личном списке маршрутизации минус вместо корзины у записи, корзина вместо метлы у очистки списка.
- Шлюз в списке «Мои устройства» рисуется своей иконкой: в текстовых списках он был 🛰, а в кнопках — обычным телефоном.
- «Адрес сервера» стал «Доменным именем» и честно пишет «не задано», когда в ссылки уезжает IP. «Имя в ссылках» стало «Именем сервера».
- Порт на экране читается у живого интерфейса, а не из конфига: именно он уезжает в ссылки. Расхождение с конфигом показывается предупреждением. Порт линка до шлюза к этому разделу отношения не имеет, о чём сказано прямо.

## v2.16.0 — списки профилей, перекладка настроек, покрытие

#all_bots
#awg_gen1

**Исправлено**
- **Сломанный вывод списков.** Карточка профиля, экран «Управлять подпиской» и список профилей падали, как только у устройства появлялся хендшейк: проверка «онлайн» получала порог секунд на месте опорного времени. Сломалось в v2.5.6, при переводе карточек на один снимок. Порог теперь передаётся только именем, позиционно туда попадает лишь дата.
- Скрипт переезда на хост больше не открывает порт клиентов через ufw. Владелец правил один — таблица бота, она сканирует ListenPort в конфигах и открывает порт сама; второй слой рядом с ней был ровно тем, от чего уходили.

**Настройки разложены заново**
- Корень основного бота: сервер AWG, доступ по SSH, условная маршрутизация, e-mail, уведомления, параметры подписок, обслуживание, обновления. Порядок — от того, что трогают при настройке сервера, к тому, что трогают раз в полгода.
- «Сервер» переименован в «Сервер AWG», «Файервол» — в «Доступ по SSH»: раздел про вход снаружи, а не про файервол вообще.
- Мониторинг и резервное копирование уехали в «Обслуживание»: корень распух до десяти строк, а открывают их не ради настройки, а когда чинят. Внутри порядок такой: мониторинг, резервное копирование, перезапуск AWG, перезапуск бота. «Назад» из перенесённых разделов возвращает в обслуживание.
- У агента шлюза то же зеркально: e-mail, уведомления, обслуживание (мониторинг и бэкапы внутри), обновления.

**Покрытие**
- 1384 → 1440 тестов. Закрыто всё, что оставалось: таймер отката и его снятие, закрепление правил от перезагрузки, состояние таблицы, неинтерактивный мастер файервола и его сообщения в чат, отправка из процессов без бота, просьба о переезде при старте, вторая половина промоушена интерфейса, частичный отказ записи конфига, гашение старого интерфейса, черновики и ассеты релизов, цикл опознания админа кодом, показ первого устройства, ветки действий файервола, сверка ключей кнопок со словарями текстов, сохранность файла секретов, уборка временного каталога.
- Семь тестов проверяли текст исходника вместо поведения и пережили бы поломку. Переписаны на запуск настоящего кода: статус ядра и решение о пересборке гоняются с подменёнными modinfo и awg, подбор обфускации проверяется двумястами прогонами на повтор и на равную длину пакетов, подмена модуля и уборка каталога исполняются настоящим bash.

## v2.15.0 — ревизия после закрытия дистрибуции

#all_bots
#awg_gen1

Полная проверка сценариев, ревью кодовой базы и покрытия после пяти поставок п.8. Ниже — то, что нашлось и исправлено.

**Ломало установку**
- Установка на хост, где AmneziaWG уже стоял (переустановка, образ с поднятым сервером), умирала на валидации «не задан network.server_port»: топология писалась в конфиг только для сервера, созданного нами. Теперь пишется в обоих случаях, а первый клиентский адрес берётся следующим за адресом сервера — у доставшегося от докерной Amnezia он `.0`, и клиенты идут с `.1`, как и раньше.
- IPv6 в списке адресов SSH ломал раздел «Файервол» навсегда: адрес уезжал в callback_data, где двоеточие — разделитель полей, и раздел переставал открываться, а убрать запись из чата было нечем. В кнопке теперь номер записи, а не адрес.

**Ломало обновление**
- Вторая половина обновления падала молча: сервис уже остановлен, вывод уходит в никуда, бот просто исчезал. Теперь при любом отказе сервис поднимается на том, что есть, и в чат приходит объяснение.
- Правка ключа, которого нет в конфиге старой установки, обрывала обновление насмерть — с подменённым кодом и неподнятым сервисом. Внутри обновления такие правки стали предупреждением.
- Копия скрипта обвязки в `/usr/local/sbin`, которую юнит выполняет при каждой загрузке, не обновлялась никогда: исправления применялись руками, а после перезагрузки хост поднимался по версии времён установки. Теперь копия обновляется вместе с поставкой.
- `awg-bot update` без аргумента искал `awg-bot-update.tgz` — имя, которого не существует ни в релизе, ни в сборке. Теперь принимает и `awg-bot.tgz`.

**Ложные тревоги и тупики**
- Агент шлюза при каждом старте жаловался на незаданную подсеть клиентов, хотя честно берёт её из юнита обвязки. Проверка спрашивает то же место.
- Сразу после успешного развёртывания обвязки приходила тревога «шлюз не отвечает» — шлюза ещё нет. Зонд молчит, пока шлюз не назначен.
- Предупреждения про файервол звали в SSH; теперь называют раздел в боте.
- Развёртывание обвязки включает и саму функцию: иначе кнопки «Назначить шлюз», на которую отправлял итоговый текст, в разделе не было.
- Смена шлюза на другое устройство выпускала файл первого применения без токена агента, и установка на шлюзе снова начинала задавать вопросы. Токен спрашивается в обоих путях.
- Обещание «агент напишет тебе сам» невыполнимо: Telegram не даёт боту начать диалог. Текст просит отправить `/start` боту шлюза.
- Адрес для вопроса про SSH не определялся под `sudo` (переменная окружения не переживает `env_reset`). Добавлен второй источник — запись в utmp.
- Вопросы CLI файервола читались из stdin, занятого текстом скрипта: «поставить nftables?» отвечало отказом за человека, сказавшего «да».
- Код сопряжения прилетал боту повторно после старта, и первым в чате был ответ на собственный установочный код.
- Автодетект адреса за 1:1 NAT (AWS, GCP, Oracle, Azure) молча брал приватный адрес и вписывал его в каждую ссылку. Теперь распознаётся и заменяется внешним.
- Обвязка хоста без юнита dnsmasq останавливалась с кодом успеха, и бот рапортовал «✅ развёрнуто» при неразвёрнутой обвязке.

**Чистка**
- Убраны мёртвые ветки и тексты снятой схемы release-токенов, неиспользуемые импорты, недостижимый статус шлюза. Статический анализ продуктового кода чист.
- Дефолты в коде разошлись с шаблоном конфига (каталог awg, первый адрес пула, keepalive) — выровнены. Дефолт режима в обвязке хоста был `docker` против `host` у бота.
- Документация приведена в соответствие: раскладка подсети, докерная схема как рудимент, вопросы установщика, `--rekey`, раздел «Ручная установка» заменён на раздел о диагностике.

**Тесты**
- 1366 → 1400+. Закрыты дыры: согласование файла состояния поколений между установщиком и ботом, сервисный слой файервола целиком (включение с таймером, откат флага при отказе, арминг только там, где он нужен, IPv6 сквозь весь путь), живое чтение серверных настроек по всем ключам, эффект развёртывания обвязки, топология при уже существующем сервере.

## v2.14.0 — шлюз без вопросов, обвязка ВПС кнопкой

#all_bots
#awg_gen1

Пятая, последняя поставка дистрибуции с чистого хоста (docs/ROADMAP.md, п.8). Пункт закрыт целиком.

**Основной бот**
- Раздел «🇷🇺 Условная маршрутизация» показывается всегда. Пока обвязка не развёрнута, он и есть место, где её разворачивают: кнопка «🚀 Развернуть» ставит dnsmasq, настраивает перехват DNS, NAT и маршруты клиентов, поднимает линк-туннель, закрепляет всё от перезагрузки и вписывает интерфейс в конфиг. Раньше это были три команды в SSH из README плюс правка app.yaml руками, и ни одна из них не проверяла, что предыдущая отработала.
- Любой шаг не отработал — обвязка не применяется наполовину, причина показывается текстом. После успеха бот перезапускается сам: имя интерфейса линка читается при старте.
- «➕ Новая машина» спрашивает токен бота шлюза — один раз — и запоминает его. Переустановил машину, сменил шлюз: файл первого применения выпускается заново без похода в @BotFather. Сообщение с токеном из чата удаляется.
- После выпуска файла бот показывает ровно две команды для машины-шлюза: `scp` и установку агента.
- Устройство «Шлюз» больше не упирается в лимит устройств админа. Шлюз из лимита исключён по смыслу, но флаг ставился строкой позже, и админ с выбранным лимитом не мог завести себе шлюз вовсе.

**Агент шлюза**
- Установка не задаёт ни одного вопроса. Токен агента и `ADMIN_ID` лежат внутри файла первого применения; установщик ищет его в `/root` (или берёт путь из `--bundle`), применяет (аплинк, линк, обвязка, файервол шлюза) и только потом ставит агента — тому нужен уже поднятый аплинк, иначе Telegram недоступен.
- Нет файла — установка честно останавливается и печатает обе команды, а не советует почитать документацию.
- Секреты лежат в файле ПОСЛЕ `exec`: при запуске бандла они не исполняются, это данные для установщика, которые он читает `sed`.

## v2.13.1 — установка одной командой

#all_bots
#awg_gen1

- Установка стала настоящим однострочником: `curl -fsSL …/install/awg-bot-install.sh | sudo bash`. По ссылке едет не архив, а сам установщик; из трубы он качает поставку во временный каталог, сверяет её sha256 с релизом и **передаёт управление установщику из архива**. Логика установки принадлежит поставке и едет вместе с ней: выполняйся на хосте код из ветки `main`, а ставься релиз — расходились бы они молча.
- Аргументы передаются после `-s --`, например `| sudo bash -s -- --role gateway`.
- Все вопросы визарда читаются из `/dev/tty`, а не из stdin. В трубе stdin занят текстом скрипта: обычный `read` прочитал бы его вместо ответа человека и молча ушёл по умолчанию — с токеном бота из случайной строки кода.
- Сорвалась установка (не root, не та система, отказ визарда) — временный каталог убирает тот, кто его создал. Через `exec` ловушку не унести, поэтому установщик из архива запускается дочерним процессом, а уборка идёт по коду возврата.
- Прежний путь (скачать архив, распаковать, запустить `install/awg-bot-install.sh`) работает как работал.

## v2.13.0 — поставка одним архивом

#all_bots
#awg_gen1

Четвёртая из пяти поставок дистрибуции с чистого хоста (docs/ROADMAP.md, п.8). Установленным ботам ничего не меняет: самообновление берёт тот же `awg-bot.tgz`, что и раньше.

- Поставка — **один артефакт**. Установщик переехал внутрь архива: прежние «скачай два файла и не перепутай версии» кончились, рядом с архивом класть больше нечего.
- Установка одной командой: архив скачивается во временный каталог, распаковывается, установщик запускается оттуда. Распаковывать код второй раз незачем — он уже вокруг, поэтому в `/opt/awg-bot` он просто раскладывается.
- Скачанный архив сверяется по sha256 с релизом GitHub. Сервис не ответил — ставим дальше и говорим об этом строкой (загрузку и так прикрывает TLS); не совпало — установка отказывается начинаться. Для своей сборки есть `--skip-verify`.
- Установщик убирает за собой архив и каталог распаковки. Каталог — только временный (`/tmp`, `/var/tmp`): поставку могли распаковать и в домашний каталог, удалять её там мы не вправе. Проверка пути стоит дважды, по разные стороны от `rm -rf`.

## v2.12.1 — подтверждение файервола приходит в чат

#main_bot
#awg_gen1

- Применение правил файервола из установщика или CLI теперь само присылает в чат сообщение с кнопками «Вход работает, подтверждаю» и «Откатить сейчас». Прежде оно советовало выполнить `awg-bot firewall confirm` — то есть отправляло человека в тот самый SSH, который правила могли только что отрезать. Команда осталась как запасной путь.
- Сработавший таймер отката тоже сообщает о себе в чат. Раньше откат происходил молча в транзиентном systemd-юните, и админ оставался уверен, что файервол включён, хотя он выключен.
- Отправка сообщений из процессов без бота (установщик, CLI, таймер) — новый модуль `awgbot/infra/tgsend.py`: один POST на стандартной библиотеке, ошибки гасятся, установка от недоступного Telegram не падает.

## v2.12.0 — установка в один вопрос

#main_bot
#awg_gen1

Третья из пяти поставок дистрибуции с чистого хоста (docs/ROADMAP.md, п.8). Установленным ботам ничего не меняет: правки касаются установщика и добавляют два раздела в настройки.

**Установка**
- Вопрос остался один — токен бота. Кто админ, установщик выясняет сам: печатает одноразовый код, ждёт его в чате бота и берёт id отправителя. Ходить за числовым Telegram ID к стороннему боту больше не нужно. Не вышло (нет связи, прервали) — спросит `ADMIN_ID`, как раньше.
- Топология не спрашивается: внешний адрес автодетектом, имя сервера «Сервер 1», таймзона из системы, порт и подсеть уже записаны при создании сервера.
- Шифрование бэкапов из визарда убрано: экран есть в чате.
- Файервол — один вопрос с подставленным ответом: «Ограничить доступ по SSH адресом, с которого вы сейчас подключены (…)? [y / свой список адресов / N]». Применяется с таймером отката на 15 минут, подтвердить можно в боте.
- В конце установки конфигурация первого устройства печатается прямо в терминал: ссылка, QR и путь к файлу. Telegram в этот момент может не открываться — за этим VPN и ставят. Отдельно доступно командой `awg-bot first-device`.
- `--advanced` возвращает все прежние вопросы.

**Бот**
- Раздел «🖥 Сервер»: адрес сервера, имя в ссылках, DNS клиентов, MTU. Значения читаются в момент выдачи ссылки, поэтому правка применяется сразу, без рестарта; уже выданные конфиги не меняются. Порт, подсеть, интерфейс, версия ядра и поколение показываются, но не правятся — их смена означает перевыпуск профилей всем.
- Раздел «🛡 Файервол»: адреса для входа снаружи, добавление и удаление, включение и выключение фильтра. Включение и удаление адреса идут с таймером отката, и подтверждать его теперь можно в чате, а не вторым SSH-сеансом: чат работает независимо от того, заперт SSH или нет.
- Ввод адреса сервера и DNS проверяется до записи: значение уезжает в каждую следующую ссылку, а сломанную ссылку человек увидит только при импорте.

## v2.11.0 — поколения AmneziaWG в обновлении

#all_bots
#awg_gen1

Вторая из пяти поставок дистрибуции с чистого хоста (docs/ROADMAP.md, п.8). Поколение ядра прежнее, поэтому это обновление ничего не пересобирает и переезда не требует: оно вводит сам механизм.

**Обе роли**
- Обновление бота теперь отвечает и за ядро. Модуль с тулзами собираются из новой поставки ДО подмены кода: не собралось — код не подменён, версия прежняя, сервис поднят обратно. Изменился тег модуля — работающий подменяется собранным, все awg-интерфейсы гасятся и поднимаются обратно (секунды простоя).
- Первое обновление после v2.10.0 усыновляет текущее поколение в `/etc/awg-bot/awg.state`. Ничего пересобирать не будет: тег тот же.

**Основной бот**
- Поставка с ядром нового поколения сама поднимает второй интерфейс рядом со старым (свободное имя, свободная подсеть, параметры нового поколения) и записывает ключи переезда в `app.yaml`. Бот при каждом старте просит начать переезд профилей, пока он не начат.
- Обновление, ушедшее дальше цели идущего переезда, недоступно: профили на трёх интерфейсах мигрировать нечем. Экран «Обновления» показывает причину, уведомление о такой версии не приходит до финала.
- Финал переезда переставляет основной интерфейс: новый становится основным в конфиге вместе с подсетью, портом и идентификатором протокола, старый гасится и снимается с автозагрузки, бот перезапускается. Раньше новые устройства продолжали рождаться на старом интерфейсе — на тех самых параметрах, ради ухода от которых затевался переезд.
- Идентификатор протокола в ссылках стал атрибутом интерфейса: старые профили сохраняют прежний, двойники получают новый.
- Хэштег `#awg_genN` в теле релиза объявляет поколение поставки. Его нет — поколение прежнее.

## v2.10.0 — AmneziaWG версии поставки и сервер с нуля

#all_bots

Первая из пяти поставок дистрибуции с чистого хоста (docs/ROADMAP.md, п.8). Установки прежней схемы (бот при докерной Amnezia) ничего не замечают: обновление ядро не трогает, сервер не пересоздаёт, раскладку подсети не меняет.

**Обе роли**
- `install/awg.lock` — версия AmneziaWG прибита к поставке: теги модуля ядра и `amneziawg-tools`, их sha256, поколение протокола. Установщик ставит ровно её, из исходников; PPA не подключается никогда, чтобы `apt` однажды не поднял ядро мимо бота.
- Модуль ставится DKMS-деревом **под своей версией** (`/usr/src/amneziawg-<версия>`), а не поверх апстримного `1.0.0`: сборки сосуществуют, откат возможен, при смене ядра через `apt` модуль пересобирается сам. Сборка — явным перебором всех установленных ядер: `dkms install` без `-k` покрывает только текущее.
- `awg-bot awg status | install | reload | plan`. `status` отвечает кодом 3, когда установленное расходится с манифестом; `reload` подменяет работающий модуль установленным, погасив и подняв все awg-интерфейсы.

**Основной бот**
- На чистом хосте установщик создаёт сервер сам: ключи, случайный высокий порт, обфускация, `ip_forward`, автозагрузка `awg-quick@awg0`. Докерная Amnezia больше не нужна.
- Новая раскладка подсети: сервер `.1`, клиенты с `.2`. Адрес `.1` оставлен под приватный резолвер условной маршрутизации. У прежних установок сервер на `.0` — их конфиг не мигрирует.
- Ключи установщика `--port N` и `--subnet X.Y.Z` задают порт и подсеть создаваемого сервера без входа в режим подробных вопросов.
- NAT клиентов переехал в nft-таблицу бота `awg_bot_guard`: она существует и при выключенном файерволе (форма NAT-only, без фильтра), `awg-bot firewall off` снимает фильтр, но не выход наружу. Обвязка условной маршрутизации своих правил MASQUERADE больше не ставит, если NAT ведёт бот; доктор принимает оба варианта.

## v2.9.0 — шлюз: владение через ключи, без токенов

#all_bots

Обновить обоих: основной бот несёт новые сценарии и скрипт обвязки (он едет в бандле), агент — отчёт после применения и чтение подсети из юнита. После обновления перевыпустить конфигурацию шлюза («⚙️ Конфигурация шлюза») и применить у агента: только новый бандл несёт исправленный скрипт.

**Основной бот**
- «🛰 Назначить шлюз» / «🔁 Сменить шлюз» — из двух вариантов: **из моих устройств** (устройство админа, уже стоящее на машине) или **новая машина** (бот создаёт устройство «Шлюз», выпускает новые ключи линка и отдаёт открытый файл первого применения). Смена на другую машину меняет ключи линка: прежняя теряет линк сама.
- «🛑 Убрать шлюз» (и «🛑 Не шлюз?» в карточке): флаг снят, ключи линка новые, условная маршрутизация выключена. Токены `release`, подтверждения замены и отдельные сообщения старому шлюзу убраны.
- Раздел показывает состояние шлюза одной строкой: 🟢 работает / ⏳ конфигурация выпущена, жду хендшейка / 🔴 не отвечает — вместо инструкций.
- Финал переезда профилей с назначенным шлюзом сразу выдаёт конфигурацию для двойника.
- Пересланный токен `claim` остаётся запасным путём при пустом шлюзе; при назначенном другом шлюзе отвергается с отсылкой в настройки.
- `routing-link-setup.sh --rekey`: новые ключи линка, конфиг и бандл заново.
- Скрипт обвязки на чистой машине (аплинков нет) ставит аплинк из бандла под именем по умолчанию `awg0`; кто применил бандл — тот и шлюз.

**Агент**
- После применения — человеческий итог из статуса скрипта («Аплинк обновлён и поднят, линк поднят, шлюз подтверждён»), хвост вывода только при отказе.
- Установщик роли gateway больше не спрашивает подсеть клиентов ВПС: агент берёт её из юнита обвязки, куда её кладёт бандл; `gateway.client_subnet` остаётся ручным переопределением.
- Приём `release` убран.

## v2.8.3 — токен шлюза одним сообщением

#all_bots

- Оба бота ловили токен `GW1:` только в начале текста, а пересланное сообщение несёт пояснение перед ним: агент молча не принимал `release`. Теперь токен ищется в любом месте текста.
- Сообщение с токеном в обоих случаях одно: пояснение и токен вместе, с кнопкой «Скрыть». Отдельного сообщения с голым токеном больше нет.

## v2.8.2 — назначение шлюза из настроек

#main_bot

- Раздел «Условная маршрутизация» без назначенного шлюза показывает «🛰 Назначить шлюз» вместо «Конфигурация шлюза»: выбор устройства админа из списка, подтверждение, и конфигурация для бота шлюза выпускается сразу, отдельного нажатия не нужно. С назначенным шлюзом раздел показывает его имя и прежнюю кнопку.
- После приёма пересланного токена от агента и после подтверждённой замены шлюза конфигурация тоже выдаётся сразу.
- Итого путь шлюза: назначить, переслать файл агенту, применить. Пересылка токена остаётся запасным путём, когда устройство выбирать некому.

## v2.8.1 — аплинк шлюза: PostUp в нужной секции и откат

#all_bots

Обновить надо ОСНОВНОГО бота: скрипт обвязки шлюза едет на малину внутри бандла из поставки ВПС, и только новый бандл несёт исправление. В v2.8.0 скрипт обвязки дописывал PostUp аплинка в конец файла, то есть в `[Peer]`, `awg-quick` отдавал их `setconf`, аплинк не поднимался, а вместе с ним пропадала связь агента с Telegram.

- PostUp/PostDown аплинка вставляются внутрь `[Interface]`, сразу за `Table = off`.
- Не поднялся аплинк с новым конфигом — скрипт сам возвращает прежний конфиг из резервной копии и поднимает его; агент остаётся на связи и сообщает об ошибке.
- Статус пометки пишется до рискованных действий.

## v2.8.0 — устройство-шлюз

#all_bots

Порядок: сначала основной бот, потом агент, потом перевыпуск конфигурации шлюза и применение у агента. Агент после применения пришлёт сообщение для пересылки основному боту, тот пометит устройство; после пометки нужен ещё один перевыпуск и применение, тогда шлюз получит стандартный конфиг аплинка из бандла вместо рукописного (те же ключи, `Table = off`, правило по метке). Аплинк при этом переподнимется, агент на секунды потеряет Telegram.

**Основной бот**
- Тип устройства «шлюз»: 🛰 в списках, онлайне и потреблении, всегда первой строкой, в счёт онлайна не входит. Не считается в лимите устройств и трафике профиля, лимит устройства не действует, не блокируется никакой причиной, нельзя удалить, передать, отдать другу, выдать ссылку, QR или файл. Шлюз один: гарантирует уникальный индекс.
- Карточка шлюза: строка устройства, потребление, «Это устройство — шлюз условной маршрутизации…»; кнопки «✏️ Имя», «⚙️ Конфигурация шлюза», «🛑 Не шлюз?» с подтверждением (снимает флаг, выключает условную маршрутизацию, выдаёт сообщение для агента).
- Пометка по пересланному сообщению агента: токен подписан ключом линка, повтор отвергается, только устройство профиля админа. Замена шлюза с подтверждением и сообщением `release` для бота старого шлюза.
- Бандл несёт ключ и конфиг аплинка помеченного шлюза (в окне переезда — двойника и старый ключ). Раздел условной маршрутизации показывает шлюз или предупреждает, что он не помечен.
- Переезд профилей: флаг переходит к двойнику на финале, новый конфиг шлюза едет перевыпуском бандла.

**Агент шлюза**
- После применения бандла сверяет ключ своего аплинка с помеченным: не помечен или помечен другой — присылает сообщение для пересылки основному боту; при чужом шлюзе линк не поднимается, пока эту машину не пометят и не перевыпустят конфигурацию. Принимает `release` от основного бота: опускает линк, выключает автозапуск обвязки. Статус пометки в панели.
- Скрипт обвязки ставит конфиг аплинка из бандла только машине с тем же ключом, с резервной копией прежнего, переподнимает аплинк лишь при изменении. Ключ `gateway.uplink_interface` задаёт аплинк явно, по умолчанию он находится по хосту `Endpoint` линка.

## v2.7.3 — реконсиляция по диффу и состояние без пустых записей

#all_bots

Поведение не меняется, работы под капотом меньше. Пересборка конфигурации шлюза не требуется.

- **Условная маршрутизация (ВПС)**: реконсиляция каждый тик читает состав всех наборов одним `ipset save` и цепочку маркировки одним `iptables -S`, а пишет только расхождение. Раньше каждые три минуты пересобирались все наборы и цепочка целиком, около 23 тысяч exec в сутки, и цепочка очищалась 480 раз в сутки. Разбор вывода структурный (пары `--match-set`, метка, порядок правил), чужая форма правила или нечитаемый снимок означают пересборку, как прежде. Восстановление снесённых снаружи правил по-прежнему в пределах тика.
- **Состояние (оба бота)**: `set_state` не пишет неизменившееся значение, счётчики стриков алертов упираются в порог вместо бесконечного роста, стрики тика собираются в одну транзакцию, пустая транзакция не коммитится. Спокойный тик больше не пишет на диск: минус около 10 тысяч записей в сутки на хост, для SD-карты малины это ресурс.
- Пороги и тексты алертов те же: алерт ровно на пороге и один раз, отбой на пороге.

## v2.7.2 — исключение из маскарада для линка ставит обвязка хоста

#main_bot

Трогает обвязку ВПС: после обновления перезапустите `awg-bot-routing.service` (или `routing-host-setup.sh --apply`). Шлюз не затронут.

- Трафик клиентов в линк уходил с адресом ВПС (`10.99.99.1`): исключение из MASQUERADE для линка ставил скрипт линка, а обвязка хоста при реассерте вставляла свой MASQUERADE выше него. Теперь `routing-host-setup.sh` сам исключает из маскарада линк и интерфейс переезда, после маскарада, то есть всегда выше. Для условной маршрутизации порядок был безразличен (шлюз маскарадит и подсеть линка), для доступа устройств админа в домашнюю сеть — нет.

## v2.7.1 — устройствам админа открыт шлюз и домашняя сеть

#all_bots

Трогает обвязку обоих хостов: после обновления **перевыпустите конфигурацию шлюза** и примените её на шлюзе, а на ВПС повторно примените `routing-host-setup.sh` (или перезапустите `awg-bot-routing.service`).

- **Шлюз**: устройствам админа (`ADMIN_IPS` из бандла, прежнее имя `SSH_ALLOW` принимается) с туннеля открыто всё — сама машина (SSH, OMV, торрент-клиент) и домашняя сеть за ней; прочим клиентам по-прежнему изоляция и drop. Set переименован в `admin4`, локальные добавки — `ADMIN_IPS_EXTRA`.
- **ВПС, найдено трассой**: MASQUERADE клиентской подсети срабатывал и для трафика пир → пир, устройство админа приходило к шлюзу с адресом сервера (`10.x.x.0`) и упиралось в drop. `routing-host-setup.sh` ставит исключение из маскарада для `-o <awg-интерфейс>`.
- **ВПС, изоляция пиров**: в таблице `awg_bot_guard` пир → пир внутри awg-интерфейсов разрешён только устройствам админа; остальные клиенты друг до друга и до шлюза по туннелю не достают. Set `ssh_tunnel4` переименован в `admin4`.
- **`routing.home_subnets`** в app.yaml основного бота: домашние подсети за шлюзом, бот держит маршрут в линк в основной таблице (`ensure_policy`). Без него пакет админа на `192.168.x.x` уходил бы в интернет ВПС.

## v2.7.0 — файервол шлюза в одной точке

#all_bots

Правка трогает обвязку шлюза: после обновления обоих ботов **перевыпустите конфигурацию шлюза с ВПС и примените её на шлюзе** (🛰 Шлюз → Конфигурация шлюза).

**Шлюз**
- Вся обвязка малины — одна nft-таблица `inet awg_gw_guard` вместо россыпи iptables: MASQUERADE клиентов и линка в WAN, изоляция клиентов от приватных сетей, метки Telegram для агента. Ставит её `routing-gw-setup.sh` из бандла, перевыставляет юнит при каждой загрузке до подъёма линка; применение атомарное, синтаксис проверяется до записи.
- **Закрыта дыра**: изоляция жила только в FORWARD, а пакет клиента на адрес самой малины (линк `10.99.99.2` или её клиентский адрес в туннеле) шёл в INPUT без фильтра — sshd, OMV, SMB, торрент-клиент были доступны любому клиенту ВПН. Теперь с адресов туннеля на шлюз пускаются только ВПС по линку (SSH, ICMP) и SSH с устройств админа, остальное дропается до рукопожатия. Домашняя сеть, NAS и чужие правила (docker, домашняя схема) не трогаются.
- Прежние правила iptables (`AWGLINK_FWD`, MASQUERADE, метки в mangle) скрипт снимает сам, и при `--rollback` тоже.
- Агент проверяет таблицу одним `nft -j list table`: подсеть клиентов в ней, цепочки, диапазоны Telegram, политика чужого FORWARD (docker-овский DROP перекрыл бы транзит). Пропала таблица — перевыставляет рестартом юнита, не чаще раза в 10 минут. На старте предупреждает, если обвязка старого образца.
- `awg-bot firewall` на шлюзе: `status`, `allow`/`deny <ip>` (локальные добавки к SSH через туннель, `/etc/awg-gw/firewall.env`), `apply`.

**Основной бот**
- Бандл шлюза несёт список устройств админа (`SSH_ALLOW`): им открыт SSH на шлюз через туннель. Состав запоминается при сборке; изменился — бот напоминает перевыпустить конфигурацию (один раз на каждое расхождение).

## v2.6.1 — мастер файервола на чистом хосте

#main_bot

- `awg-bot firewall setup` на хосте без пакета `nftables` предлагает поставить его через apt (как делал прежний `harden_firewall.sh`), а не падает с «nft не найден». Новые установки тем самым проходят тот же путь, что и обновлённые.

## v2.6.0 — файервол хоста в одной точке

#main_bot

Четверо ворот на пути SSH из туннеля (nft-таблица скрипта, цепочка iptables `AWGBOT_SSH`, PostUp-страж в conf, `Match Address` в sshd) и ufw рядом сведены в **одну** nft-таблицу `awg_bot_guard`, которую ведёт бот. Пересборка конфигурации шлюза не требуется.

- **Таблица целиком под ботом** (`infra/nftguard.py`): политика INPUT drop; открыты UDP-порты всех awg-интерфейсов (`ListenPort` каждого conf — клиенты, переезд, линк до шлюза), DNS с подсетей туннеля, `firewall.open_tcp/open_udp`; SSH — устройствам админа из туннеля (set `ssh_tunnel4`, бот правит его по событию и сверяет каждый тик), с вайтлиста `firewall.ssh_allow` (IP/CIDR/имя DynDNS — имя резолвит бот), остальным **drop до TCP-рукопожатия**. FORWARD (host-режим) — established и подсети туннеля; блокировки и отказ DoT работают поверх.
- **Fail-closed с загрузки**: файл `/etc/nftables.d/awg-bot-guard.nft` грузит `nftables.service` до подъёма интерфейсов. PostUp-строки больше не нужны. Применение всегда атомарное, сверка по тику — сравнение текста с файлом и одно чтение set.
- **`awg-bot firewall …`**: `setup` (мастер с таймером отката 3 мин), `confirm [--disable-ufw]`, `status`, `allow`/`deny`, `apply`, `off`, `rollback`. Без `confirm` таблица снимается сама — самозапереться нельзя. `install/harden_firewall.sh` стал обёрткой над `setup`.
- **Старые ворота снимаются самим ботом** после включения таблицы: PostUp с `AWGBOT_SSH` из conf интерфейсов и цепочка `AWGBOT_SSH` с джампами (разово, с меткой в state). `Match Address` в sshd_config уберите руками (README §6b).
- **conf/app.yaml**: новый блок `firewall` (`enabled`, `ssh_allow`, `open_tcp`, `open_udp`); правки применяются без рестарта. Бот предупреждает на старте, если файервол не включён, вайтлист пуст, имя не резолвится или рядом активен ufw.
- **После обновления** файервол ВЫКЛЮЧЕН (`firewall.enabled: false`), пока не выполнен `awg-bot firewall setup`; до этого SSH из туннеля держат прежние слои как есть. Порядок переключения — README §6b.
- Docker-режим: SSH из туннеля по bridge-подсети целиком (пер-пирно хост различить пиров не может), FORWARD остаётся docker'у.

## v2.5.6 — быстрее: панель, тики, рассылки

#all_bots

Пачка оптимизаций. Поведение то же, работы под капотом — в разы меньше; пересборка конфигурации шлюза не требуется.

**Основной бот**
- Панель администратора и карточка профиля собираются одним снимком из одного потока вместо 8–15 переключений и двух десятков запросов — «В меню» и открытие карточки заметно живее.
- Переход между экранами: активное меню и история — одной транзакцией; итог операции (продление, рассылка, запись в настройках) больше не даёт заведомо провального запроса на снятие кнопок.
- `/start` убирает старые меню одним запросом (`deleteMessages`), а не по одному — раньше у админа это могло занимать до десяти секунд.
- Опрос трафика: одна выборка и две пакетные записи вместо трёх–четырёх запросов на каждое устройство; сэмпл переписывается только при изменении счётчиков.
- Проверка лимитов, сроков и месячный сброс — в транзакциях, метки уведомлений точечным запросом, кандидаты по сроку отбираются в SQL; индексы на срок подписки и на близнецов переезда.
- Тик живости шлюза только измеряет; обвязка (`ip route`/`iptables`) утверждается при смене вердикта и раз в 5 минут, а не каждые 30 секунд.
- Тик монитора не ходит в БД из event loop: состояние ресурсов, сервисов и сервера — одним вызовом в рабочем потоке, `last_server_ok` пишется только при изменении.
- Проверка почты для выхода из паузы пропускается, пока ни у кого нет активной паузы с кодом.
- Восстановление блокировок после рестарта — одним `iptables -S`, добавляются только недостающие правила.
- Уведомления и рассылки уходят параллельно (окно 6) с пейсингом; тихие часы считаются один раз на пачку. Рассылка на 50 адресатов — секунды вместо полуминуты.
- Middleware держит короткий кэш «кто это» (15 с, сбрасывается любой записью в БД); посторонние больше не гоняют запросы к БД каждым сообщением.
- Счётчики устройств через `COUNT`, состояние переезда и список интерфейсов в памяти; окно переезда — с фильтром в SQL и джиттером.
- Мелочи: имя хоста читается один раз; адресаты рассылки не перечитываются на каждый тап; экран «Обслуживание» и список сирот — одним запросом; правила SSH сравниваются структурно (без пересборки из-за порядка опций у iptables-nft).

**Агент шлюза**
- Состояние линка — одним `awg show dump` вместо двух; маркировка Telegram проверяется одним `iptables -S` вместо восьми `-C`, и не дважды за тик.
- Версии, покрытие ядра и SMART кэшируются на 45 минут; кнопки «Статус» и «Монитор здоровья» всегда читают заново.
- Загрузка CPU — по дельте между тиками, без четвертьсекундного сна; в панели теперь среднее за интервал.
- Тик агента: ~7 exec вместо ~44.

## v2.5.5.4 — «Статус» и строка свежести у агента

#gw_bot

- «🔄 Обновить» на панели агента → «🔄 Статус», как у основного бота; действие
  прежнее — живой снимок.
- Строка свежести снимка — курсивом в скобках: «<i>(обновлено 2 мин назад)</i>».

## v2.5.5.3 — уведомления агента

#gw_bot

- «E-mail при недоступности Telegram» — первой кнопкой раздела уведомлений
  агента, как у основного.

## v2.5.5.2 — раздел уведомлений

#main_bot

- «E-mail при недоступности Telegram» — первой кнопкой раздела.
- События профилей («Активация профиля», грейс-период, превышение лимита,
  бонусный объём) — в подменю «👥 События профилей».

## v2.5.5.1 — восстановление без лишних рестартов

#all_bots

- **Восстановление трогает только изменившееся.** Настройки и env кладутся
  лишь те, что отличаются от копии; конфиги awg-интерфейсов — только
  изменившиеся, и переподнимаются только они. Совпали с текущими — интерфейсы
  не перезапускаются вовсе. База подменяется всегда.
- **Предупреждение об обрыве — только когда интерфейсы затронуты**, и текстом
  экрана «Перезапустить AWG» своей роли: у основного про сервер и коннекты, у
  агента про линк и РФ-доступ.
- **Приём конфигурации на шлюзе — так же**: если конфиг линка в файле не
  отличается от установленного, скрипт обвязки линк не переподнимает, а экран
  «Получена конфигурация» говорит об этом и без предупреждения; изменился —
  предупреждение экрана «Перезапустить AWG» агента.

## v2.5.5 — восстановление из файла в чате

#all_bots

- **Метка внутри архива** (`state/backup-meta.json`: роль, время снятия, хост,
  версия). Дата копии берётся из неё, не из имени файла. **Копия основного бота
  разворачивается только на основном, копия агента — только на агенте**: и бот,
  и `awg-bot restore` отказывают на чужой роли.
- **Восстановление из чата**: пришли боту файл копии (`.tgz.enc`, открытый
  `.tgz` тоже принимается) — он расшифрует своей парольной фразой, покажет дату
  и спросит подтверждение с предупреждением, что все изменения после копии
  (профили, устройства, подписки, ключи) откатятся. «Восстановить» запускает
  `awg-bot restore --yes` вне процесса бота: сервис останавливается, данные
  подменяются, конфиги awg-интерфейсов кладутся и интерфейсы переподнимаются,
  бот стартует и присылает «✅ Восстановлено из резервной копии от …».
- Инфобокс «Резервное копирование» переписан: доставка и подсказка, как
  восстановиться.
- `awg-bot backup` тоже пишет метку; `awg-bot restore --yes` — без вопросов.

## v2.5.4.1 — тексты бэкапа на почту

#all_bots

- «📨 Резервная копия отправлена на ящик …» — без счётчика файлов: файл теперь
  всегда один.
- Тема письма: «awg-bot-main: резервная копия dd.mm.yyyy hh:mm» у основного,
  «awg-bot-gw: …» у агента.

## v2.5.4 — резервная копия одним архивом

#all_bots

- **Одна копия — один файл** в обеих ролях: `awg-bot-backup-<main|gw>-<дата>.tgz`,
  при заданной фразе — `.tgz.enc` целиком. Внутри `state/bot.db`,
  `state/conf/*.yaml` (все настройки, из UI и не из UI), `state/env` и
  `awg/<iface>.conf` — у основного серверный конфиг, у агента все конфиги
  интерфейсов. Прежде агент слал 14 отдельных файлов.
- **`awg-bot restore <файл>`** разворачивает и старый снимок, и новый архив, и
  шифрованный (спросит фразу). Конфиги awg-интерфейсов кладёт после отдельного
  подтверждения; `awg-bot backup` собирает ту же раскладку.

## v2.5.3.2 — инструкция к файлу шлюза удаляется вместе с файлом

#main_bot

- «В меню» на файле конфигурации шлюза удаляет теперь и экран «что
  произойдёт»: после ухода файла ему в чате делать нечего.

## v2.5.3.1 — исправление бандла; фраза бэкапов в бандле

#all_bots

⚠️ После обновления обоих ботов **выпусти конфигурацию шлюза заново и примени
её на агенте**: файл из 2.5.3 клал на шлюз пустой скрипт обвязки.

- **Ошибка в 2.5.3**: строка с почтой вставлялась не перед строкой-маркером, а
  в sed-выражение с тем же текстом внутри бандла; sed ломался, конвейер прятал
  отказ, на шлюз ложился пустой `/opt/awg-gw/routing-gw-setup.sh`, и «готово»
  приходило от пустого файла. Вставка теперь строго перед строкой-маркером,
  а бандл отказывает, если скрипт обвязки не извлёкся.
- **Парольная фраза шифрования бэкапов едет в бандле** вместе с почтой. Без
  своей фразы агент ставит её молча; если на шлюзе уже задана другая —
  вопрос «Перезаписать / Оставить свою», всё остальное применяется в любом
  случае.
- «✉️ E-mail» — первым пунктом настроек в обоих ботах.

## v2.5.3 — почта у агента шлюза

#all_bots

- **Агент**: раздел «✉️ E-mail» (подключить, проверить, тестовое письмо,
  сменить, отключить — тот же мастер, что у основного), выбор канала бэкапа
  «Telegram | E-mail» и тумблер «E-mail при недоступности Telegram» в
  «Уведомлениях»: критичные алерты (мёртвый линк, нет выхода наружу, обвязка,
  питание, перегрев, перегруз) уходят письмом, когда Telegram не отвечает.
- **Настройки почты едут в конфигурации шлюза**: основной бот вкладывает их в
  файл для агента, агент при применении сохраняет ящик себе. Вводить дважды
  на двух хостах не нужно; файл, как и раньше, шифрован ключом линка.
- Основной бот: «Резервная копия (2 файла) отправлена…» — со склонением и без
  точки; мастер почты вынесен в общий модуль обеих ролей.

## v2.5.2 — шифрование бэкапов из чата

#all_bots

- В «💾 Резервное копирование» обоих ботов сразу под рубильником — кнопка
  «🔐 Шифрование: ✅ включено / 🔴 выключено». На экране — состояние, правила
  и «Задать/Сменить фразу»: парольная фраза принимается сообщением дважды,
  сообщения удаляются, обратно фраза не показывается никогда. Смена фразы не
  перешифровывает старые копии.
- Секрет хранится в БД; `BACKUP_KEY`/`BACKUP_PASSPHRASE` из env переезжают
  один раз при старте, бот напоминает удалить строки. Ключ из env действует,
  пока не задана фраза.
- Почтовый канал бэкапа и бэкап агента по-прежнему требуют шифрования — теперь
  отсылают к этому экрану, а не к env.

## v2.5.1 — бэкап на почту; критичные алерты по почте

#main_bot

- **Резервное копирование**: под рубильником выбор канала «☑️ Telegram | ☑️ E-mail»
  (по умолчанию Telegram). Почтой уходят только шифрованные копии, вложениями на
  ящик бота; без настроенной почты бот предлагает её настроить, без ключа
  шифрования — объясняет, что задать. Автобэкап при сбое почты падает обратно
  в Telegram.
- **Уведомления**: тумблер «E-mail при недоступности Telegram». Когда Telegram не
  отвечает (сеть, блокировка), на ящик бота уходят <b>только критичные</b>
  алерты — падение VPN-сервиса, недоступность или поломка обвязки шлюза
  маршрутизации, перегруз хоста. Остальное по почте не дублируется. Без ящика
  тумблер предлагает настроить почту.
- Исправлен общий тумблер настроек: у ключей с дефолтом «выключено» первое
  нажатие записывало «выкл».

## v2.5.0.1 — тексты раздела E-mail; подсказки провайдеров

#main_bot

- «Сменить ящик» открывает экран смены с текущим адресом, а не «Подключение».
- Ввод адреса для писем с кодом объясняет, что это и зачем, показывает текущий
  и принимает «-» для возврата к самому ящику.
- Тестовое письмо: «отправлено на ящик <адрес>» и кнопка «Скрыть».
- Серверы IMAP/SMTP на экране — моноширинным.
- Подсказки на шаге пароля сверены с состоянием на сентябрь 2026: Gmail —
  пароль приложения только при 2FA, не для Workspace; iCloud — app-specific
  password; Яндекс — пароль приложения и включённый IMAP, для организаций на
  своём домене с 29.06.2026 нужен Яндекс 360. Mail.ru из автоопределения убран:
  с 12.06.2026 пароль для внешних приложений там только по платной подписке.

## v2.5.0 — почта из чата

#main_bot

- **Раздел «✉️ E-mail» в настройках.** Ящик подключается мастером в чате: адрес
  → серверы сами для gmail.com, icloud.com, yandex.ru (с подсказкой про
  пароль приложения), для прочих доменов — IMAP/SMTP руками → пароль (сообщение
  удаляется сразу) → живая проверка входа по IMAP и SMTP → сохранение. В разделе
  «Проверить соединение», «Тестовое письмо», «Сменить ящик», «Отключить» через
  подтверждение.
- **Креды — в БД**, серверы и параметры — в `email.yaml`; всё горячо, без
  рестартов. Из env бот один раз переносит прежние `EMAIL_RESUME_*` и напоминает
  удалить их. Шаг про почту из визарда `awg-bot` убран.
- **Аварийный выход из приостановки** — функция на почтовом канале со своим
  рубильником, адресом для писем с кодом, интервалом опроса (с джиттером) и
  длиной кода. Опрос IMAP стартует и останавливается по рубильнику сам.
- База под следующее: уведомления админу по почте, резервный канал агента
  шлюза, бэкапы и конфиги письмом — тем же аккаунтом.

## v2.4.8.2 — без доступности линка за сутки

#all_bots

- Строка «📡 Доступность линка за сутки» убрана из обеих панелей вместе с
  учётом замеров: недоступность шлюза закрыта алертами, а лишняя метрика
  по замерам — лишний повод для поведенческого детекта. Проба наружу в
  мониторе здоровья агента остаётся.

## v2.4.8.1 — рубильник резервного копирования

#all_bots

- В «Резервном копировании» обоих ботов первой кнопкой — рубильник
  (по умолчанию включён). Выключен — остальных кнопок нет и автобэкап не идёт,
  как в условной маршрутизации. Ключ `app.scheduler.backup_enabled`.

## v2.4.8 — перезагрузка хоста, проба наружу, доступность линка, настройки агента

#all_bots

- **Оба бота**: после перезагрузки хоста — «⚠️ Хост hostname был перезагружен.
  ✅ Запуск бота/агента успешен», после всех проверок старта. По смене boot_id
  ядра: рестарт сервиса за перезагрузку не считается.
- **Оба бота**: под аптаймом — «📡 Доступность линка за сутки: N%». У агента —
  по тикам монитора, у основного — по замерам тика живости шлюза.
- **Оба бота**: джиттер у всех задач, которые ходят в сеть по расписанию
  (тик монитора шлюза с пробой наружу, живость шлюза на ВПС, проверка
  обновлений ±30 мин, обновление списков) — строго периодический запрос
  выглядит маячком.
- **Агент**: в мониторе здоровья — «выход наружу»: TCP-проба через домашний
  канал к российскому и зарубежному адресу с временем ответа; алерт с
  гистерезисом, если канал молчит.
- **Агент, настройки**: «🔔 Уведомления» (тихие часы; алерты хоста: CPU и RAM в
  одной строке, ниже диск и «Temp: XX °C»), «📊 Мониторинг» (как у основного,
  на ключах шлюза: частота опроса, стрик, громкость и порог простоя линка),
  «💾 Резервное копирование» (день и час автобэкапа, «создать сейчас»). Бэкап
  шлюза — только шифрованный: внутри приватные ключи линка; без
  BACKUP_KEY/BACKUP_PASSPHRASE агент объяснит, что задать.
- **Агент**: алерты CPU/RAM по порогам «Уведомлений» (раньше был только диск).

## v2.4.7.2 — итог продления

#main_bot

- Итог продления кнопкой: «✅ Подписка профиля N продлена на 1 месяц, до
  DD.MM.YYYY HH:MM»; для бессрочной — «✅ Период подписки профиля N успешно
  изменён.» и жирным «Подписка теперь бессрочная.». Ручная правка дат получила
  ту же ✅ в начале.

## v2.4.7.1 — «Продлить?» и строка истекающих жирным

#main_bot

- В списке истекающих подписок ссылка «Продлить?» — жирным.
- Строка «⏳ Истекающие подписки: N» на панели — жирным.

## v2.4.7 — истекающие подписки

#main_bot

- **«⏳ Истекающие подписки: N»** — последней строкой панели, только когда такие
  есть. Ссылка ведёт на список: профиль, остаток «X дней Y часов Z минут»,
  период «DD.MM.YYYY HH:MM → DD.MM.YYYY HH:MM», «Продлить?» — стандартный
  маршрут продления. После продления или отмены — обратно в список, пока он не
  пуст, иначе в меню. «В меню» — назад.
- **Правило «истекает»** — своё, не пороги уведомлений: при длине периода L дней
  остаток ≤ max(⌊L/12⌋, 7) дней (год → 30, месяц → 7); периоды короче 12 дней
  (неделя, сутки) в списке всегда; бессрочные — никогда.
- **Уведомления**: месячной подписке порог «30 дней» не показывается никогда
  (31-дневный месяц получал его назавтра после активации).
- **Итог продления больше не садится в меню**: «✅ Подписка продлена до…»
  остаётся инфосообщением, меню следом со своим обычным текстом.

## v2.4.6.1 — двоеточие в заголовке

#main_bot

- «📊 Потребление трафика за текущий месяц:» — с двоеточием.

## v2.4.6 — адресаты релизов хэштегами

#all_bots

- **Кому релиз — хэштегом**, а не дисклеймером: `#main_bot`, `#gw_bot`,
  `#all_bots` первой строкой тела релиза. Каждый бот ищет среди версий новее
  установленной **ближайшую со своим хэштегом** и пропускает чужие: агенту на
  2.4.5 при 2.4.6 `#main_bot` и 2.4.7 `#gw_bot` предложат сразу 2.4.7.
  Установленные версии основного бота и агента теперь расходятся — это норма.
- Релизы без хэштега (все прежние) считаются общими.

## v2.4.5.2 — интервалы в списках; профиль у устройства онлайн

> ✅ **Обновление шлюза не требуется** (агента — нет).

- На экранах «Устройства онлайн» и потребления между записями пустая строка:
  эмодзи в начале соседних строк налезали друг на друга, а межстрочный
  интервал Telegram не настраивает.
- Устройство онлайн — «Имя устройства (Имя профиля) — адрес».

## v2.4.5.1 — адреса моноширинным

> ✅ **Обновление шлюза не требуется** (агента — нет).

- Адреса устройств — моноширинным вместо невидимых соединителей: автоссылки
  нет, а тап по адресу копирует его в буфер.

## v2.4.5 — онлайн: статус у получателей, экран устройств онлайн

> ✅ **Обновление шлюза не требуется** (агента — нет).

- **Объявление**: у каждого профиля в списке получателей — статус справа от
  имени (🟢 хотя бы одно устройство онлайн / 🔴 никого), онлайн-профили сверху.
- **«📶 Устройств онлайн» в панели — ссылка** на экран «Устройства онлайн (N)»:
  иконка (📱 своё / 📲 передано другу), имя, адрес. «В меню» — назад.
- **Адреса устройств — текстом, не ссылкой**: Telegram линковал всё, похожее
  на IP. Строка устройства: «🟢 Имя (10.9.1.2), последний коннект: …».

## v2.4.4.4 — заголовок экрана потребления профиля

> ✅ **Обновление шлюза не требуется** (агента — нет).

- «📊 Потребление профиля N за текущий месяц:» — короче.

## v2.4.4.3 — экраны потребления; кнопки передачи по ролям

> ✅ **Обновление шлюза не требуется** (агента — нет).

- Экраны потребления: «📊 Потребление трафика за текущий месяц» и «…профиля N
  за текущий месяц:»; профили с «👤», устройства — с той же меткой, что в
  списке устройств у админа (онлайн, блокировка, «не ботом»).
- **Карточка устройства**: «👤 Передать другу» — только владельцу, «🔀 Передать
  в другой профиль» — только админу. У админа на чужом устройстве были обе.

## v2.4.4.2 — экран потребления на месте панели; разбивка по устройствам

> ✅ **Обновление шлюза не требуется** (агента — нет).

- Переход по ссылке из панели **заменяет саму панель** экраном потребления,
  как переход по кнопке: меню исчезает, «В меню» возвращает его на то же место.
- Разбивка по устройствам **падала** на профиле с устройствами: не то поле
  трафика у модели устройства. Тест теперь с реальным устройством и трафиком.

## v2.4.4.1 — без превью ссылок в меню

> ✅ **Обновление шлюза не требуется** (агента — нет).

- Ссылки в панели и экранах потребления рисовались с превью-вложением
  Telegram. Превью отключено во всех навигационных экранах: меню, экраны по
  кнопкам, панель после перезапуска.

## v2.4.4 — потребление за месяц по профилям и устройствам

> ✅ **Обновление шлюза не требуется** (агента — нет: правка в основном боте).

- **«📊 Потребление за месяц (все)» в панели — ссылка.** Открывает экран с
  разбивкой по профилям; имя каждого профиля — тоже ссылка, на разбивку по его
  устройствам. Из профилей — «В меню», из устройств — «Назад» к профилям.
- Кликабельный текст в сообщении бота бывает только ссылкой, поэтому переход
  идёт deep-link'ом на самого бота: нажатие отправляет служебную команду
  `/start traffic…`, бот тут же удаляет её из чата и показывает экран.

## v2.4.3.7 — hostname в заголовке панели

> ✅ **Обновление шлюза не требуется** (агента — нет: правка в основном боте).

- Заголовок панели администратора — «🛠 Панель администратора (hostname)»,
  как у агента шлюза.

## v2.4.3.6 — «Скрыть» у агента

> ✅ **Обновление шлюза не требуется** (агента — да).

- У агента не было обработчика кнопки «Скрыть», которая стоит на каждом
  уведомлении монитора, на предупреждениях при старте и в уведомлении о новой
  версии: нажатие уходило в «not handled», уведомления было не убрать.
  Подключён, работает как у основного бота — удаляет сообщение.

## v2.4.3.5 — юнит бота на автозагрузке

> ✅ **Обновление шлюза не требуется** (агента — да, и основного тоже: правка общая).

- **Найдено ребутом малины: агент не поднялся, юнит был `disabled`.** При
  установке роли gateway включение и запуск шли одной командой с запасным
  «просто перезапустить»; упавший первый старт (Telegram через туннель ещё
  недоступен) уводил в запасную ветку, и автозагрузка терялась молча.
  Теперь включение отдельно от запуска, при каждом обновлении юнит бота
  включается идемпотентно для обеих ролей, а бот при старте предупреждает,
  если он не включён.

## v2.4.3.4 — монитор здоровья агента без кэша

> ✅ **Обновление шлюза не требуется** (агента — да).

- После обновления модуля awg монитор здоровья десять минут показывал
  «Модуль awg: ?» и «ядро без модуля»: кэш статических проб запомнил
  середину операции, когда модуль был снят с диска. Кэш убран — панель и так
  рисуется из снимка тика, а `modinfo` с обходом одного каталога стоят
  копейки. «Монитор здоровья» и «Обновить» всегда смотрят живьём.

## v2.4.3.3 — автозагрузка интерфейса в host-режиме

> ✅ **Обновление шлюза не требуется** (агента — нет: правка касается основного бота на хосте).

- **Найдено ребутом ВПС: `awg1` не поднялся сам.** В докерном режиме интерфейс
  поднимал контейнер; в host-режиме его поднимает `awg-quick@<interface>`, и
  этот юнит никто не включал. Теперь `awg-bot.sh` включает его на каждом
  обновлении, а бот при старте предупреждает, если автозагрузки нет.
- **Юнит и таймер `awg-bot-lists` прежних версий** указывали на удалённый
  скрипт и падали при каждой загрузке: списки давно обновляет сам бот.
  Скрипт обновления их убирает; бот предупреждает, если они остались.

## v2.4.3.2 — перезапуски в основном боте через подтверждение

> ✅ **Обновление шлюза не требуется** (агента — тоже: правка только в основном боте).

- «🔄 Перезапустить AWG» и «🔄 Перезапустить бота» в «Обслуживании» больше не
  срабатывают с одного нажатия: сначала экран с ценой действия и кнопками
  «Отмена | Выполнить» — как у агента шлюза.

## v2.4.3.1 — агент: тексты зеркально основному

> ✅ **Обновление шлюза не требуется** (агента — да).

- «🔄 Обслуживание» и «⬆️ Обновления бота» у агента — те же тексты, что у
  основного бота (с текущей версией последней строкой).
- «К панели» везде → «В меню»; в подтверждениях «Не надо» → «Отмена».
- Монитор здоровья: модуль awg и загружаемые ядра — отдельными строками.

## v2.4.3 — агент шлюза: новая панель и меню

> ✅ **Обновление шлюза не требуется** (агента — да: вся правка в нём; имя ВПС в
> панели появится после повторного выпуска конфигурации шлюза с ВПС).

- **Панель агента — зеркало основной.** «🛰 РФ-шлюз (hostname)», сервер и аптайм,
  «📡 Линк до <имя ВПС>», железо (CPU и температура, RAM без свопа и свободно,
  диск со свободным местом и SMART, питание), «🌡 Монитор здоровья» одной строкой,
  «📊 Потребление за месяц» линка. Модуль awg и ядра ушли из панели в монитор
  здоровья; внешний IP упразднён вместе с его алертом — ВПС к шлюзу по адресу не
  ходит, линк поднимает сам шлюз.
- **Панель из снимка тика.** /start и «К панели» — ноль проб, мгновенно; строка
  «Обновлено …» показывает свежесть. «🔄 Обновить» ходит по пробам живьём.
- **Меню**: «Обновить | Монитор здоровья», «Мастер восстановления» (бывший
  реассерт), «Настройки» → «Обслуживание» («Перезапустить AWG», новое
  «Перезапустить бота») и «Обновления бота». Раскладка обновлений прежняя.
- **Потребление за месяц** копится по дельтам счётчиков линка: рестарт
  интерфейса итог не обнуляет, новый календарный месяц начинает с нуля.
- Имя ВПС для «Линк до …» едет в конфигурации шлюза (`SERVER_NAME`);
  переопределяется `gateway.server_name` в app.yaml.

## v2.4.2.13 — конфигурация шлюза: экран перед выпуском

> ✅ **Обновление шлюза не требуется** (агента — тоже: правка только в основном боте).

- **«⚙️ Конфигурация шлюза» больше не выпускает файл с одного нажатия.**
  Сначала экран: что именно соберётся, куда придёт, что с ним делать и что
  «В меню» удалит сообщение с файлом. На экране — «📤 Выпустить файл» и
  «✖️ Отмена» (назад в раздел). Файл уносит ключ линка — выпуск должен быть
  осознанным.

## v2.4.2.12 — текущая версия в разделе обновлений

> ✅ **Обновление шлюза не требуется** (агента — да: буква v в «Установлено»).

- В разделе «⬆️ Обновления бота» основного бота последней строкой — **текущая
  версия**: раньше её было не увидеть, не запросив проверку.
- У агента в «Установлено» версия тоже с буквой v.

## v2.4.2.11 — версии с буквой v; одна живая кнопка в окнах обновления

> ✅ **Обновление шлюза не требуется** (агента — да: правки общие для обеих ролей).

- **Версии везде с буквой v.** Голое «2.4.2.8» Telegram принимал за IP-адрес и
  рисовал ссылкой. В уведомление о новой версии добавлена строка с текущей —
  в обеих ролях.
- **Уведомление «доступна следующая версия» снимает «В меню» у прошлого
  финишера.** После ступени в чате висел отчёт «обновлён до…» с живой кнопкой,
  а под ним — уведомление о следующей: две живые кнопки в соседних окнах.
  Живой остаётся только последнее окно; тексты не трогаем.

## v2.4.2.10 — проверка ядер без шума

> ✅ **Обновление шлюза не требуется** (агента — да).

- **Проверка «ядра без модуля awg» смотрит только на ядра, в которые эта
  машина может загрузиться.** Raspberry Pi OS кладёт в один пакет ядра всех
  плат (`-v8`, `-2712`, `-v7l`) — чужие здесь не загрузятся никогда, а старые
  не нужны: назад не откатываемся. Раньше они висели в панели вечным «🔴 без
  модуля». Теперь считаются ядра того же варианта, что запущенное, и не старее
  его. Смысл проверки прежний — **новое ядро из apt до ребута**: dkms молча
  пропускает ядро без headers, и ребут в него оставил бы шлюз без awg.

## v2.4.2.9 — агент шлюза: уведомление о следующей ступени и отзывчивость панели

> ✅ **Обновление шлюза не требуется** (агента — да: обе правки в нём).

- **Уведомление о следующей версии приходило ВЫШЕ отчёта «обновлён до…».**
  Планировщик агента стартовал раньше финишера, а его стартовая проверка
  укладывается в секунду — «доступна следующая» ложилось над отчётом и
  выглядело старым. Планировщик теперь запускается строго после финишера,
  как у основной роли: сначала «обновлён», под ним «доступна следующая».
- **Панель отзывчивее.** Поиск модуля awg шёл рекурсивно по всему дереву
  `/lib/modules/*/**` — тысячи файлов на каждое ядро при каждом показе.
  Смотрим два известных места (`updates/dkms`, `kernel/net`). Статические
  пробы (`modinfo`, обход ядер) кэшируются на 10 минут: они меняются только
  руками. Доктор и монитор по-прежнему ходят живьём.
- Старт агента ~11 с на Raspberry Pi 4 — это импорт моделей Telegram
  (aiogram/pydantic) на ARM, приложением не ускоряется; на функции не влияет.

## v2.4.2.8 — раздел маршрутизации: четыре кнопки

> ✅ **Обновление шлюза не требуется.**

Раздел «Условная маршрутизация» перестроен из простыни в четыре кнопки:

1. **выключатель** функции;
2. **⚙️ Конфигурация шлюза** — файл для бота шлюза (бывший «бандл»);
3. **📋 Списки маршрутизации** — подраздел: состояние, период, «обновить сейчас»;
4. **👥 Доступность пользователям** — подраздел с переключателями профилей.

Кнопки 2–4 видны только при включённой функции. Приписки про шифрование из
интерфейса убраны — это деталь механизма, её место в документации. За списками
при выключенной функции бот не ходит.


## v2.4.2.7 — «В меню» живёт только у последнего окна обновления

> ✅ **Обновление шлюза не требуется** (агента — да: правка общая для обеих ролей).

- При цепочке ступеней кнопка «В меню» снималась только у **предыдущего**
  финишера — один id в памяти. Всё, что выпадало из цепочки («не применилось»,
  «не удалось» с той же кнопкой), оставалось живым. Теперь ведётся история всех
  окон обновления: новый финишер и нажатие «В меню» на любом из них снимают
  кнопки у всех прошлых. Тексты остаются — история ступеней ценна.


## v2.4.2.6 — один ответ на «обновить списки»

> ✅ **Обновление шлюза не требуется.**

- «Обновить списки сейчас» отвечало на колбэк дважды — Telegram показывает
  только первый ответ, и итоговое «N записей» не доходило. Теперь ответ один.


## v2.4.2.5 — списки маршрутизации в чате

> ✅ **Обновление шлюза не требуется** (и агента тоже: списки живут на ВПС).

- В разделе «Условная маршрутизация» — **блок «Списки»**: сколько записей, из
  скольких источников, когда обновлены (числом — «час назад» и «шесть дней
  назад» это разница между живыми источниками и застывшим кэшем), период.
- **Пикер периода** (3 / 6 / 12 / 24 ч) — горячий ключ, без рестарта.
- **«Обновить списки сейчас»** — принудительно, не дожидаясь окна: когда
  источник только что починили, ждать до шести часов незачем.

На шлюзе списков нет по построению — он получает уже помеченный трафик.


## v2.4.2.4 — расписание проверки обновлений у агента

> ✅ **Обновление шлюза не требуется** (агента — да).

- Раздел обновлений агента теперь повторяет основной бот полностью: **пикер
  расписания** (каждый день / раз в неделю / раз в месяц / никогда) с отметкой
  текущего, тумблер уведомлений принудительно выключен при «никогда» и не
  включается, пока расписание не сменить; выбор «никогда» сам глушит
  уведомления. Смена применяется **без рестарта** — задача перевешивается
  хуком настроек, как у клиентской роли.


## v2.4.2.3 — раздел обновлений у агента шлюза

> ✅ **Обновление шлюза не требуется** (агента — да: это про него).

- В панели агента — **«⬆️ Обновление агента»**: установленная версия,
  расписание проверки, тумблер уведомлений и «Проверить сейчас». Раньше была
  только реакция на уведомление о новой версии: пришло до того, как открыл
  чат, или заглушено — обновляться нечем.


## v2.4.2.2 — «В меню» на бандле удаляет сообщение с файлом

> ✅ **Обновление шлюза не требуется.**

- Возврат в меню с экрана бандла **удаляет само сообщение с файлом**, а не
  снимает кнопку: после возврата он не нужен, а внутри — ключ линка (в шифре,
  но оставлять его в истории незачем). Кнопка у бандла теперь своя.


## v2.4.2.1 — /start убирает прошлые меню; бандл с кнопкой «В меню»

> ✅ **Обновление шлюза не требуется** (агента — да, у него тоже /start).

- **Повторный /start убирает ВСЕ прошлые меню чата**, а не только снимает
  кнопки с последнего. Для этого чат ведёт историю нав-сообщений (до 30);
  что Telegram удалить не даст (старше 48 ч), молча пропускается. В обоих
  ботах, для всех ролей: админ, клиент, друг, агент шлюза.
- **Бандл шлюза уходит с кнопкой «В меню»**, а экран настроек при его выдаче
  гаснет: живым остаётся одно меню — на самом бандле; нажатие снимает кнопку
  и возвращает панель.
- Панель агента шлюза переведена на общие нав-хелперы: одно живое меню,
  прошлое гаснет, история ведётся.


## v2.4.2 — роль gateway, этап 3: самообновление агента

> ✅ **Обновление шлюза не требуется** (агента — да: с этой версии он умеет
> обновлять себя сам, и это последний раз, когда его обновляют руками).

### 🎯 Что нового

- **Самообновление агента** — тем же механизмом, что у клиентской роли:
  проверка GitHub-релизов по расписанию, уведомление о следующей ступени,
  «Обновить» с кнопки, sha256 ассета, апдейтер вне cgroup сервиса, отчёт после
  рестарта с единственной живой кнопкой «В меню».
- **Механизм вынесен в общую примесь** (`SelfUpdateMixin`) без изменения
  поведения клиентской роли — держать его в клиентском классе значило бы
  тащить в агент тысячи строк чужой механики ради десяти методов.
- **Обновление ядра AWG кнопкой — не делается** (решено): ядро бывает обратно
  несовместимо, и каждое обновление требует чтения коммитов. Остаётся ручной
  процедурой; агент показывает версию, srcversion и покрытие ядер.

Пункт 7 роадмапа закрыт целиком.


## v2.4.1 — роль gateway, этап 2: операции и шифрованный бандл

> ✅ **Обновление шлюза не требуется** (агента на нём — да: этап 2 живёт в нём).

### 🎯 Что нового

- **Доктор шлюза** — все проверки панели плюс путь к Telegram и линк, по
  строкам: чинить будут по ним, а не по вердикту.
- **Рестарт линка и реассерт обвязки с кнопки** — через подтверждение с ценой
  («РФ-доступ оборвётся на секунды»); итог остаётся в чате отдельным сообщением.
- **Бандл шлюза через чат.** На ВПС в разделе условной маршрутизации — кнопка
  «Бандл для шлюза (шифрованный)»: файл приходит тебе, ты пересылаешь его
  агенту, тот проверяет и применяет. Ключ шифрования выводится из приватного
  ключа линка шлюза — общего секрета обеих сторон, который через Telegram не
  ходил никогда: наблюдатель чата видит только шифртекст, чужой ключ бандл не
  откроет. Файл с приватным ключом на диске шлюза не задерживается.
- **Маркировка Telegram реассертится агентом сама** на каждом тике (правило
  идемпотентно, хуже не сделает) и видна в докторе.
- **`--role gateway` в установщике**: `awg-bot-install.sh --role gateway` или
  `awg-bot reconfigure --role gateway` — без клиентского визарда, свой вопрос
  про подсеть ВПС, те же секреты и юнит.


## v2.4.0.2 — агент шлюза ходит к Telegram только по IPv4

> ✅ **Обновление шлюза не требуется** (агента — да, если уже поставлен).

- **Сессия агента прибита к IPv4** (`gateway.ipv4_only`, по умолчанию
  включено). Шлюз стоит там, где Telegram заблокирован, и достаёт до API только
  через туннель до ВПС — а туннель v4, тогда как резолвер отдаёт API сначала
  AAAA: aiohttp уходил по v6 в никуда и тонул в таймауте.
- Сам путь через туннель — правило маркировки диапазонов Telegram в PostUp
  клиентского конфига шлюза (см. роадмап §7); агент его пока не ставит сам —
  это этап 2.


## v2.4.0.1 — дефолт runtime: host

> ✅ **Обновление шлюза не требуется.**

- **Шаблон конфига и дефолт кода — `runtime: host`.** Докерная схема снята
  давно, но дефолт остался «docker», и на свежей машине без докера юнит
  получал `Requires=docker.service` — systemd отказывался его запускать.
  Всплыло на первой свежей установке (агент шлюза). Докерный режим остаётся
  только явным значением.


## v2.4.0 — роль gateway: агент на шлюзе (этап 1)

> ✅ **Обновление шлюза не требуется** — наоборот: на шлюз теперь МОЖНО
> поставить собственного бота-агента. Существующая обвязка не меняется.

### 🎯 Что нового

Бот получил вторую роль (`role: gateway` в app.yaml): агент на шлюзе условной
маршрутизации со своим Telegram-токеном. Этап 1 — наблюдаемость:

- **Панель**: линк до ВПС (возраст хендшейка числом, трафик), обвязка
  (MASQUERADE, изоляция LAN с порядком DROP/ACCEPT, ip_forward, юнит
  реассерта), покрытие всех установленных ядер модулем awg, железо Pi
  (температура, недонапряжение через vcgencmd, место на карте), версия и
  srcversion модуля, внешний IP.
- **Алерты с гистерезисом**: мёртвый линк (громкий, два тика), поломка
  обвязки, ядро без модуля, недонапряжение, перегрев, диск, смена внешнего
  IP (громкий: эндпоинт линка на ВПС смотрит на DDNS). «Не смог посмотреть»
  — третье состояние, не норма и не отказ: счётчики не двигает.
- Никакой клиентской механики: свой лёгкий `GatewayServices`, свой маленький
  скедулер (один тик), свой набор preflight-предупреждений. Никаких входящих
  портов, никаких каналов управления между хостами.

### Установка на малину (пока руками, установщик — этапом 2)

1. распаковать awg-bot.tgz в /opt/awg-bot, venv + юнит по образцу ВПС
   (`awg-bot.sh reconfigure --first-run` не запускать — он клиентский);
2. `/etc/awg-bot/env`: BOT_TOKEN нового бота (BotFather), ADMIN_ID тот же;
3. `/etc/awg-bot/conf/app.yaml`: `role: "gateway"` + секция `gateway:`
   (образец в шаблоне conf/app.yaml, задать client_subnet);
4. `systemctl start awg-bot` → /start в чате нового бота.

Красная линия роли: никакого терминала в боте; SSH остаётся резервом.


## v2.3.5.3 — грабли финала переезда закрыты в коде

> ✅ **Обновление шлюза не требуется.** Живой шлюз уже настроен верно; правки
> касаются будущих пересборок бандла.

### 🐞 Что нашлось при живом финале

- **Бандл шлюза возил замёрзший Endpoint.** Конфиг шлюза генерируется один раз,
  и порт в нём застывает; после переноса порта линка бандл увёз шлюз в закрытую
  дверь — линк умер до ручной правки. Теперь сборка бандла сверяет Endpoint с
  живым `ListenPort` линка и правит источник до упаковки (закреплено тестом).
- **`--bundle` не работал из `/usr/local/sbin`** — сборщик ищет gw-скрипт по
  соседству, а юниты зовут именно копию, где соседа нет. Добавлен фолбэк на
  каталог поставки.
- **`sh harden_firewall.sh` падал с невнятным «Illegal option»** — скрипт
  bash-only, а `sh` игнорирует шебанг. Теперь перезапускает себя в bash сам.
- **`routing.client_subnet` в шаблоне закомментирован**: по умолчанию он
  выводится из `network.subnet_prefix` и меняется вместе с ним. Явное значение
  замерзало и при смене подсети давало «нет маршрута до старой подсети» —
  четвёртая грабля финала, теперь невозможная на свежих установках.


## v2.3.5.2 — финал переезда переживает ребут

> ⚠️ **Обновление шлюза ТРЕБУЕТСЯ** — впервые за всё время: юнит на шлюзе
> должен запомнить новую клиентскую подсеть. Делается пересборкой бандла на
> ВПС и одним запуском на шлюзе, в шаге 5 финала.

### 🐞 Что нашлось

Подготовка финальных шагов вскрыла, что вся конфигурация окна переезда жила
«до первого ребута»:

- **юниты реассерта не знали про новую подсеть.** Все три скрипта обвязки
  держали `CLIENT_SUBNET=10.8.1.0/24` захардкоженным дефолтом, а юниты зовут их
  без переменных: после ребута DNAT, MASQUERADE и исключение линка встали бы
  для старой подсети, а новая осталась бы без DNS и NAT — молча;
- **фасадный адрес резолвера (`10.9.1.1`) был эфемерным.** Его вешали руками;
  юнит поднимает dummy только с `10.255.53.1`. После ребута адреса нет,
  dnsmasq с `bind-interfaces` не биндится и не стартует — DNS умирает у всех.

### 🎯 Что изменилось

- `routing-host-setup.sh` и `routing-link-setup.sh` читают клиентскую подсеть
  из `app.yaml` (`network.subnet_cidr`) — источник истины один, реассерт по
  ребуту всегда ставит актуальное;
- `routing-host-setup.sh` сам поднимает **фасадный резолвер**: если
  `client_config.dns1` приватный, адрес встаёт на dummy, dnsmasq его слушает,
  INPUT его пропускает. Ручной костыль в `dnsmasq.d` больше не нужен;
- бандл шлюза **вшивает** клиентскую подсеть (на шлюзе нет `app.yaml`), а юнит
  шлюза закрепляет её строкой `Environment` — смена подсети = новый бандл.


## v2.3.5.1 — отчёт о рассылке не пересказывает объявление

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### 🎯 Что изменилось

- **Отчёт — только факт**: «✅ Объявление выше доставлено…» плюс строка о
  недоставленных, если они были. Ни текста, ни числа вложений: само объявление
  остаётся в чате строкой выше, и пересказывать то, что видно глазами, значит
  удваивать каждую рассылку в истории.
- **След текстовой рассылки сохранён**: раньше её превью редактировалось в
  отчёт, и текст жил именно там — теперь превью редактируется в чистый текст
  объявления (срезается хвост «Отправляем?» и кнопки), а отчёт-факт приходит
  следом. Симметрично альбомному пути, где выше остаётся альбом.


## v2.3.5 — объявления с картинками

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### 🎯 Что нового

- **К объявлению можно приложить до 10 картинок** — уйдёт ОДНИМ сообщением:
  альбом, текст под ним (технически — подписью к первому вложению, именно так
  Telegram склеивает альбом с текстом в один пост).
- **Собирается одним действием**: выбери снимки и набери текст прямо в окне
  отправки вложений. Порядок не важен — текст можно прислать и до, и после
  картинок: превью пересобирается, прежнее удаляется из чата. Пачка апдейтов
  альбома даёт ровно один рендер.
- **Превью с картинками — само объявление**, отправленное админу ровно в том
  виде, в каком уйдёт людям; блок подтверждения приходит следом (reply_markup
  у альбома не бывает). Без текста превью строится так же, а блок прямо
  спрашивает про пустую подпись — объявление из одних картинок легально.
- **Лимиты названы заранее, и их два**: 4096 символов без картинок, 1024 с
  ними (текст едет подписью, а длинная подпись — привилегия Premium-аккаунта,
  которым бот быть не может). Сверх лимита не отправляется: отказ называет и
  фактическую длину, и потолок, черновик — картинки и адресаты — сохраняется.
  Лимит перепроверяется на итоговом черновике и на кнопке «Отправить»:
  картинка, добавленная после длинного текста, меняет потолок задним числом.
- **Тупиков в диалоге нет**: каждая отбивка черновика несёт кнопку «Отмена».
  Отмена удаляет и превью — иначе в чате осталась бы запись, неотличимая от
  разосланной. Отчёт об отправке различает объявление без текста.
- **Цепочка ступеней self-update**: живая кнопка «В меню» — только у
  последнего финишера, у прежних она снимается при отправке следующего.

Кнопки «Скрыть» у объявления с картинками нет: у альбома не бывает клавиатуры.

## v2.3.4 — переезд: мгновенное поздравление, подтверждения и SSH на оба интерфейса

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### 🎯 Что нового

- **Поздравление о переезде приходит за секунды.** Отдельный частый тик окна
  переезда (настраивается, `scheduler.migration_watch_seconds`) ловит первый
  хендшейк двойника; вне окна и после переезда всех тик не делает ни одного
  вызова. Право поздравить разыгрывается атомарно — дублей не бывает.
- **Старт переезда подтверждается**, как оба выхода: с его секунды меняется
  выдача, и в подтверждении заранее назван состав когорты. Начало, отмена и
  завершение остаются в чате отдельными сообщениями — экран настроек
  переписывается навигацией, а «когда начали и чем кончилось» спрашивают потом.
  «Двойники» ушли из видимых текстов — везде «новые профили».
- **`subnet_address` в `vpn://` считается из адреса устройства**, а не из
  глобальной подсети: у второго интерфейса своя сеть, а поле вморожено в
  выданную ссылку. Для старой подсети выдача побайтово прежняя.

### 🔐 SSH-к-хосту в окне переезда

Все слои фильтра знали один интерфейс — и каждый отказывал молча, выглядя
исправным. Теперь:

- правила `AWGBOT_SSH` ставятся на каждый интерфейс, глухие DROP-ы — после ВСЕХ
  разрешений; страж fail-closed — в conf каждого интерфейса;
- джамп в цепочку обязан стоять ПЕРВОЙ строкой INPUT и возвращается на место,
  если его сдвинула чужая вставка;
- `harden_firewall.sh` сам добавляет подсеть переезда в SSH-вайтлист хоста
  (отдельная nft-таблица: accept в одной не отменяет drop в другой);
- документированы четвёртые ворота — `sshd` (`Match Address` знает подсети
  поимённо) — и порядок разбора отказов: счётчики цепочки → `ssh_allow4` →
  `sshd -T -C` под конкретный адрес.

### 📋 Финальные шаги переезда дополнены

`network.subnet_prefix` переключается вместе с интерфейсом; старая подсеть
вычищается из всего, что её помнит (`sshd_config`, `ssh_allow4`, правила DNS и
MASQUERADE); клиентская подсеть шлюза меняется на обеих его сторонах одним
заходом — пока подсети разные, шлюз работает по совпадению (трафик переехавших
маскарадится в адрес линка), а в финале это совпадение исчезает.

## v2.3.3 — поздравление приходит сразу, а не по расписанию

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### 🎯 Что изменилось

- **Поздравление рождается в момент, когда опрос увидел первый хендшейк**, а не
  на ближайшем тике монитора. Прежняя схема опаздывала дважды — сначала ждала
  опроса, потом отдельного обхода, — и до восьми минут человек сидел уже
  подключённым, но не поздравленным.
- **Периодический обход всех устройств убран.** Он существовал ровно затем,
  чтобы не пропустить момент подключения. Момент не пропускается: переход
  «хендшейка не было → появился» опрос уже отслеживает, потому что пишет
  значение ТОЛЬКО при изменении.
- **Таблица отметок `migration_greeted` удалена** и снимается миграцией с
  боевых баз. Она хранила «кого уже поздравили», чтобы обход не повторялся —
  а без обхода повторяться нечему: нужный переход по определению случается один
  раз. Оставить её пустой значило бы оставить вопрос «зачем это?» тому, кто
  откроет схему через полгода.

Побочно: подключение на СТАРОМ пире поздравления не вызывает — это не переезд,
а обычное пользование тем, что у человека и было.

## v2.3.2 — человек узнаёт, что его устройство переехало

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### 🎉 Что появилось

- **Поздравление по первому подключению на новых настройках.** Приходит один раз
  на устройство, в момент, когда двойник впервые сделал хендшейк. Вторая строка
  просит удалить старый профиль из приложения — это единственное, что от
  человека требуется, и сказать об этом надо, пока он ещё в приложении: сам
  старый профиль никуда не денется, а после завершения переезда просто
  перестанет работать.
- **Адресат — тот, у кого конфиг на руках.** Для расшаренного устройства это
  ДРУГ, а не владелец: владелец его отдал, в приложении оно у друга, и просьба
  удалить старый профиль осмысленна только для него.
- **В тихие часы уходит беззвучно.** Событие приятное, но реакции не требует —
  будить ради него незачем.

Отметка «поздравлен» ставится ДО отправки: проверка идёт на каждом тике
монитора, и потерянное сообщение здесь лучше повторяющегося. Отметки живут одно
окно переезда — следующий поздравит заново.

## v2.3.1 — keepalive перестал быть метрономом

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> ℹ️ **Действует на НОВЫЕ конфиги.** Боевой `app.yaml` при обновлении не
> мигрирует — правь `client_config.keepalive_seconds` руками, если хочешь того
> же на работающем сервере. Уже выданные конфиги не меняются сами: значение
> живёт в них, а не на сервере.

### 🔇 Что изменилось

- **`keepalive_seconds` в поставке стал диапазоном `"25-35"`.** Фиксированные
  25 секунд — метроном ванильного WireGuard: на простаивающем туннеле пакет
  уходит ровно по расписанию, и это видно без всякой расшифровки. Прочие
  таймеры протокола (`RekeyAfterTime` и соседи) джиттерит сам сервер и они
  приезжают клиенту с него, а `PersistentKeepalive` — единственный, который
  задаёт бот. Оставшись числом, он обнулял бы всю ломку ритма самым заметным
  сигналом.

  Диапазон разбирается тулзами AmneziaWG 3.x (`u16_range_from_string`) и
  совпадает с дефолтом приложения. Клиенту дореформенного поколения нужно
  одиночное число — значение осталось настраиваемым.

## v2.3.0.2 — переехавшие оставались без условной маршрутизации

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> 🔻 **Обязательно, если переезд уже запущен.** У переехавших устройств РФ-режим
> не работал вовсе, и отказ был молчаливым.

### 🩹 Починено

- **Хук маркировки покрывал только одну клиентскую подсеть.** Он собирался
  константой `-s ROUTING_CLIENT_SUBNET` на импорте модуля, поэтому трафик из
  подсети переезда до цепочки маркировки не доходил вообще. Отказ обманчивый:
  туннель работает, DNS отвечает, интернет есть — а российские сервисы видят
  зарубежный адрес. То есть не работает ровно то, ради чего функция и
  существует, и связать это с переездом неоткуда. Теперь хук ставится на каждую
  подсеть, а частично поставленный набор считается отсутствующим — иначе
  рубильник показывал бы рабочее состояние, пока половина людей идёт мимо.
- **«Включено на 6 из 12» при шести устройствах.** Счётчик устройств профиля
  считал сырым SQL по всем строкам, а в окне переезда их вдвое больше. Теперь
  по видимым — флаг у пары синхронный, поэтому счёт по видимой половине точен.

### Чем это было упущено

Доктор `_check_static_plumbing` я на две подсети развёл, а сам механизм
маркировки — нет: проверка стала честнее того, что проверяет. Оба дефекта
закрыты тестами, и первый из них ловит именно молчаливый случай — что хук
существует, но не для всех.

## v2.3.0.1 — рычаг переезда не отвечал на нажатие

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> 🔻 **Ставить обязательно, если включён переезд профилей.** В 2.3.0 кнопки
> раздела переезда не работали вовсе: нажатие крутило спиннер и не делало
> ничего.

### 🩹 Починено

- **Обработчик переезда был зарегистрирован после общего.** Фильтры проверяются
  в порядке регистрации, а у `do_action` он широкий (`F.act == "do"`) — он и
  перехватывал все колбэки раздела. Ключ не подходил ни к одной его ветке,
  функция заканчивалась молча, ответа на колбэк не было, и Telegram крутил
  анимацию до таймаута. В журнале это выглядело как `Duration 2 ms` без единой
  ошибки. Соседний `routing_action` стоит выше `do_action` ровно по этой
  причине — правило было, и оно оказалось нарушено.
- **Порядок закреплён тестом.** Прежние тесты звали функцию обработчика
  напрямую, а при прямом вызове маршрутизация не участвует — поймать такое они
  не могли в принципе. Новый проверяет порядок в самом роутере и падает, если
  специфичный обработчик окажется ниже общего.

## v2.3.0 — переезд профилей на второй интерфейс

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> 💤 **Фича спит, пока не заданы ключи.** `docker.migration_interface` и
> `docker.migration_subnet_prefix` в `app.yaml` пустые по умолчанию — рычага нет,
> строк прогресса нет, бот ходит на один интерфейс ровно как раньше. Обновление
> безопасно ставить, ничего не готовя.

### 🚚 Зачем это

Всё, ради чего затевается перевыпуск профилей — ротация обфускации, новые ключи
поколения 3.1, смена порта и адреса резолвера, — это параметры уровня
ИНТЕРФЕЙСА. Два набора на одном интерфейсе не живут, поэтому переезд «на месте»
означал бы флаг-день: в момент переключения падают все, кто не успел. Причём
падают ровно те, до кого потом не достучаться — у кого Telegram работает через
этот же туннель.

Отсюда конструкция: рядом со старым интерфейсом поднимается новый, каждому
устройству рождается двойник, человек переимпортирует конфиг когда ему удобно, а
старый пир всё это время работает.

### Что появилось

- **Устройство знает свой интерфейс.** Колонка `devices.iface`, пустая строка —
  интерфейс по умолчанию: боевая база мигрирует без бэкфилла. Это не «режим
  миграции», а постоянное обобщение — ротация обфускации по своей природе
  повторяемая, и второй раз писать уже ничего не придётся.
- **Пара смоделирована явно** — `devices.twin_of`. На ней держатся прогресс,
  слияние истории и парные операции; по имени пару не собрать.
- **Рычаг в настройках → обслуживание** с двумя выходами. Завершение роняет
  непереехавших и потому называет их ПОИМЁННО до нажатия — тем же расчётом,
  которым потом отработает. Отмена коннекты переехавших НЕ рвёт: они остаются
  жить и собираются админу отдельным списком.
- **Прогресс двумя строками** — в панели админа и в карточке профиля (только
  админу). Строка кликабельна и открывает список отставших поимённо.
- **Уведомление о готовности** — один доклад на смену состояния, с инструкцией,
  что делать дальше.

### Что предусмотрено внутри

- **Двойник наследует всё, что молча ломается**: имя, блокировку, режим РФ-доступа,
  лимит, гостевой доступ. Невыбранный инвайт-код ПЕРЕНОСИТСЯ, а не копируется —
  поиск по коду делает `fetchone` по неуникальной колонке, и с двумя строками под
  одним кодом активация попала бы в неопределённую.
- **Мутации парные**: удаление, блокировка, переименование, перенос между
  профилями, лимит, тумблер РФ-режима. Тронуть одну строку значит оставить
  призрачный доступ или тумблер, который ничего не переключает.
- **Сверка идёт по конфигу своего интерфейса.** Без этого каждый пир соседнего
  интерфейса становился «пропавшим», и бот удалял их сам.
- **Опрос трафика обходит все интерфейсы**, лимит в окне считается суммой по паре.
- **`host_ssh_targets` знает оба**: админ переезжает первым, и с адресами одного
  интерфейса он терял бы SSH-в-туннель ровно после собственного переезда.
- **Генератор понимает ключи 3.1** — `RandomTrailers`, `HeaderProtectionKey`,
  `ContentPaddingAddition`, джиттер-таймеры. Набор и порядок сверены с
  исходниками приложения 5.0.1.5, а не с одним образцом конфига. Правка инертна:
  пока сервер ключей не задаёт, выдача побайтово прежняя.
- **Версии из четырёх цифр** больше не ломают обновления (`2.3.0.1`).

## v2.2.3.1 — автозапуск обвяза больше не зависит от того, откуда его ставили

> 🔻 **Обновление шлюза ТРЕБУЕТСЯ, и оно же чинит уже установленное.** Скрипты обвяза прописывали в systemd-юнит сами себя по пути запуска — то есть по временному каталогу, куда распаковалась поставка. Через десять дней `systemd-tmpfiles` вычищает `/tmp`, и автозапуск умирает молча. Уже поставленные шлюзы и хосты в этом состоянии находятся прямо сейчас, и обнаружится это при первой же перезагрузке.
>
> Починка — перезапуск скрипта **с постоянного места**; он перепишет юнит правильным путём:
> ```
> tar -xzOf awg-bot.tgz ./install/routing-gw-setup.sh > /usr/local/sbin/routing-gw-setup.sh
> chmod 0755 /usr/local/sbin/routing-gw-setup.sh
> /usr/local/sbin/routing-gw-setup.sh --apply /etc/amnezia/amneziawg/awglink.conf
> ```
> На ВПС то же самое для `routing-host-setup.sh --unit` и `routing-link-setup.sh`, если их юниты тоже смотрят во временный каталог: `systemctl cat awg-bot-routing awg-link | grep ExecStart`.

### 🩹 Починено

- **Юнит ссылался на каталог, откуда запустили скрипт.** `ExecStart` собирался из `readlink -f "$0"`, а поставка шлюза распаковывается во временный каталог. Отказ был устроен так, чтобы его нельзя было заметить: интерфейс уже поднят, `RemainAfterExit=yes` держит юнит «активным», сам скрипт больше не зовётся — и всё выглядит рабочим ровно до перезагрузки. После неё за шлюзом пропадает интернет, без связи с каким-либо действием и без строки в журнале, кроме `203/EXEC`. Шлюз при этом стоит в чужом доме за NAT.
- **Скрипты ставят себя в `/usr/local/sbin` до записи юнита** и подставляют в `ExecStart` уже этот путь. Повторный запуск с постоянного места копирования не делает — `install a a` затёр бы файл, который в этот момент исполняется.
- **Исправлено во всех трёх местах**, где эта заготовка повторялась: `routing-gw-setup.sh` (шлюз), `routing-host-setup.sh` и `routing-link-setup.sh` (ВПС). На ВПС не выстрелило только потому, что там их запускали из постоянного каталога, — но гарантией это не было.

## v2.2.3 — состояние, которое умеет чиниться само

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> ℹ️ **Версии теперь бывают из четырёх цифр** — `2.2.3.1` для заплатки поверх выпущенного. Понимает это только код начиная с текущей версии: боты на 2.2.2 и старше такой тег не увидят вовсе.

### 🩹 Починено

- **Маршрутизация оставалась выключенной ещё до шести часов после того, как всё починилось.** Самопроверка кэшировала вердикт бессрочно, а сбрасывался кэш ровно в одном месте — при обновлении списков, раз в шесть часов. Пока окружение менял сам бот, этого хватало; но оно меняется и снаружи: обновили ядро, перезапустили линк, поправили руками на хосте. Вердикт «интерфейс не найден», вынесенный на минуту простоя, переживал устранение причины и держал фичу выключенной у всех пользователей. Теперь отрицательный вердикт перепроверяется раз в минуту, положительный кэшируется как прежде — цена ошибок разная: ложное «не работает» не ловится ничем, ложное «ок» ловится зондом живости.
- **Бот обещал вернуться после перезапуска и не возвращался.** Сообщение «вернётся через несколько секунд» оставалось в чате мёртвым, без кнопок, пока админ сам не отправлял `/start`: обещание давал уходящий процесс, а исполнить его было некому — после старта в чат никто не пишет. Теперь новый процесс подменяет то самое сообщение отчётом «бот успешно перезапущен» — он остаётся в чате единственным следом того, что перезапуск состоялся, а не завис, — и следующим сообщением присылает панель. Перезапуск не из чата — ребут хоста, падение, `systemctl` руками — по-прежнему проходит молча.
- **В журнал пишется смена вердикта маршрутизации, а не каждая проверка.** Иначе «фича неактивна» сыпалось бы раз в минуту и хоронило под собой ту единственную строку, где отказ начался. Возврат к рабочему состоянию теперь тоже строка — прежде отказ в журнале никогда не заканчивался.

### 🔧 Версии

- **Разбор версии понимает четвёртую цифру** — `2.2.3.1`. Прежде такой тег не разбирался, и последствий было два, оба тихих: тег выпадал из списка релизов, то есть обновление никому не предлагалось, а бот, на который такую версию поставили руками, не находил себя среди тегов и **выключал обновления навсегда**. Сравнение опирается на кортежи разной длины (`2.2.3 < 2.2.3.1 < 2.2.4`), поэтому дополнять короткую версию нулём нельзя — `2.2.3` и `2.2.3.0` должны остаться разными.

## v2.2.2 — доклад об источнике списков называет настоящую причину

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### 🩹 Источники списков маршрутизации

- **Доклад обвинял файл там, где до файла не дошли.** `fetch` отдавала пустоту при любой неудаче, и «не достучались» приходило в учёт тем же нулём записей, что и «ответили пустым». Различить их дальше было нечем, поэтому совет всегда звучал одинаково: проверь, не переехал ли файл. На 429 от GitHub это отправляло искать целый и никуда не девавшийся файл, пока чинить надо было связь — а точная причина в это время лежала в логе.
- **Причина теперь в самом сообщении**, и она же определяет совет. 404 и 410 — файл переехал или удалён, нужен новый адрес в `app.routing.lists_home_urls`. 429, 5xx, таймаут, DNS — не дошли мы, обычно проходит само. Ответ 200, из которого нечего разобрать, — третий случай со своим текстом.
- **Тревога уходит не по первой неудаче**, а после первой попытки и трёх доборов с паузой в пять минут. Сеть моргает, а лимит GitHub живёт минуты: доклад по одному промаху приучил бы не читать эти сообщения ровно к тому разу, когда источник умрёт по-настоящему.
- **Отказ больше не съедает окно обновления целиком.** Метка времени ставилась при любом исходе, поэтому после неудачи следующее обращение приходилось на шесть часов спустя — добор попыток был невозможен в принципе, а сообщение о восстановлении отставало от самого восстановления на те же шесть часов.
- **О восстановлении бот говорит вслух.** Молчащий источник не ломает маршрутизацию сегодня, поэтому увидеть своими глазами, что списки снова обновляются, неоткуда, — и разошедшийся сам собой лимит оставался бы висеть нерешённым делом. Смена одного диагноза на другой докладывается тоже: меняется место, куда идти чинить.

## v2.2.1 — объявления доходят как написаны

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> ⚠️ **Если v2.2.0 уже стоит — обновиться стоит.** Объявления в ней уходили без форматирования, и заметить это до отправки было нельзя: превью показывало ту же потерю.

### 🩹 Объявления

- **Форматирование доходило до адресатов потерянным.** Жирный, курсив и ссылки, сделанные средствами Telegram, живут не в тексте сообщения, а в его entities, — а читали мы текст. Превью честно показывало то, что уйдёт, поэтому сверка перед отправкой ничего не ловила. Побочно перестала ломать разбор обычная угловая скобка в тексте.
- **Отмена оставляла черновик висеть в чате** — и приглашение «пришли объявление», и само сообщение с текстом. Теперь набор черновика стирается целиком, как переписка любого другого диалога. При отправке — тем более.
- **Отчёт об отправке остаётся в чате.** Превью превращается в него и никуда не девается: это единственный след разосланного. Копии у отправителя нет — Telegram показывает ему свои реплики боту, но не то, что бот разослал другим, — поэтому в отчёт входит и текст объявления целиком, с форматированием. Недоставленные названы отдельной строкой: «доставлено» при неудачах было бы неправдой.
- **Панель администратора приходит следующим сообщением**, со своим обычным текстом и статусами, а не поверх отчёта.
- **«Отправить» и «Отмена» переставлены местами.** Необратимое действие не должно стоять там, куда палец идёт по инерции.

### 💄 Мелочи интерфейса

- **Счётчик устройств пишется дробью** — `6/10`, безлимит `6/∞`. Прежде админ читал «6/без ограничения», а владелец профиля про то же событие — «6 подключённых устройств (без ограничения)».
- **«Потребление не ограничено»** вместо «в рамках лимита профиля не ограничено (профиль без ограничений)», когда лимита нет ни на устройстве, ни на профиле: прежняя оговорка намекала на лимит, которого нет.
- **В уведомлении о выданном устройстве появился QR-код** — способы выдачи там были неполными, хотя обработчик есть, — и добавлена кнопка «Скрыть», как на любом уведомлении, которого человек не заказывал.

## v2.2.0 — режим на устройство, объявления с выбором адресатов

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.
>
> 🔻 **Миграция необратима по состоянию.** Колонка `clients.routing_master` удаляется, а режим переезжает на устройства. Откат на предыдущую версию режим не сломает, но **выключит его у всех** — флаги на устройствах уцелеют, однако старый код их не читает. Включать придётся заново из бота. Бэкап БД перед обновлением: `cp -a /var/lib/awg-bot/bot.db /root/bot.db.bak`
>
> ℹ️ **Адресное объявление переехало.** В v2.1.1 оно жило кнопкой в карточке профиля; теперь вход один — с главной админа, а адресаты выбираются отдельным шагом.

### ⚡ Условная маршрутизация — на устройство, а не на профиль

- **Режим включается по устройствам.** Раньше он был свойством профиля целиком, и это ломалось там, где в одном профиле оказывались устройства с разными требованиями — например, домашний шлюз, которому маршрутизация противопоказана, рядом с телефоном, которому она нужна. Обойти можно было только заведением второго профиля ради одного флага.
- **Переключатели живут на отдельном экране**, куда ведёт кнопка из раздела РФ-доступа. В карточке устройства их нет намеренно: там они терялись бы среди выдачи конфигов, лимитов и блокировок, а охват профиля не был бы виден нигде. На экране всё состояние сразу и массовое действие первой строкой.
- **Состояние профиля больше не хранится, а выводится**: «включено» ⇔ включено хоть на одном устройстве. Пока оно жило и на профиле, и на устройствах, эти двое были обязаны совпадать, а свестись обратно при расхождении им было негде.
- **Новое устройство приходит выключенным**, даже если у профиля режим включён. Наследовать «включено» опаснее: устройство, которому маршрутизация не нужна, начало бы ходить через шлюз молча. Промах в обратную сторону виден на счётчике «включено N из M».

### 📢 Объявления

- **Вход один — с главной админа.** Кнопка в карточке профиля убрана: она отвечала на вопрос «кому», который логичнее задавать явным шагом, и лезла в глаза там, где админ занят совсем другим.
- **Адресаты выбираются галочками**, с множественным выбором: объявление обычно касается нескольких профилей сразу, и гонять весь путь по разу на каждого значило бы рассылать один текст руками N раз.
- **Получатели названы поимённо** и на шаге ввода, и в превью. Между выбором и подтверждением стоит ввод текста, и к моменту отправки легко забыть, кого отметил, — а ошибка необратима: лишний адресат объявление уже прочитал.
- **Про гостевой доступ предупреждаем, только когда друзья среди адресатов реально есть.** Предупреждение на каждом объявлении перестаёт читаться ровно к тому разу, когда оно важно.

### 🩹 Починено и подчищено

- **Профиль администратора убран из списка «Профили».** Весь его функционал всегда есть на главной, а карточка была урезана до тех же кнопок — строка в списке дублировала главную и путала, кто тут кем управляет. Заодно исправлен возврат из раздела РФ-доступа: он вёл в эту несуществующую карточку.
- **Массовая кнопка делала не то, что обещала.** «Выключить все» показывалось при любом частичном включении, а нажатие гасило уже включённое вместо того, чтобы довести набор до полного.
- **Согласование по числу.** «Выключено на всех 1 устройстве», «Получат: Ксюша — владельцы этих профилей» и подобное — вычищено.
- **Выданный `.conf` не грузился на линуксовых клиентах** — пустые поля обфускации `I2`–`I5` роняли `awg setconf`. Вошло ещё в v2.1.1, но там осталось незамеченным для тех, кто не ставил конфиг на шлюз или роутер.

## v2.1.1 — адресные объявления

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась.

### ✨ Новое

- **Объявление одному профилю.** В карточке профиля у админа появилась кнопка
  «📢 Объявление пользователям» — прямо над блокировкой, потому что предупредить
  и применить должны быть под рукой в один момент. Уходит владельцу профиля и
  тем, кому он раздал устройства: их это касается напрямую, а узнать иначе
  неоткуда. Остальные профили объявления не видят. В карточке самого админа
  кнопки нет — адресатом там был бы отправитель.

### 🩹 Починено

- **Отмена рассылки не сбрасывала ввод.** Прежде отмена вела прямо в главное
  меню, чей обработчик чистит состояние попутно, — и на этом всё держалось.
  Адресная рассылка возвращает в карточку профиля, где такой уборки нет:
  передумавший на шаге ввода админ оставался в режиме набора объявления, и
  следующее его сообщение боту молча становилось черновиком.
- **Анти-дабл-тап стал поадресным.** Общий запрещал бы объявить одно и то же
  двум профилям подряд, а это не двойное нажатие, а обычная работа.
- **Выданный `.conf` не грузился ни на одном линуксовом клиенте.** Эталон
  приложения содержит поля обфускации `I2`–`I5` пустыми, и телефон это глотает, а
  `awg setconf` падает с `Line unrecognized` и не поднимает интерфейс вовсе. То
  есть конфиг годился только для телефона: ни шлюз, ни роутер, ни второй сервер
  его не принимали. Пустые поля больше не печатаются — для `awg` отсутствие ключа
  и пустое значение равнозначны. Во встроенный в `vpn://` конфиг они по-прежнему
  идут: формат заморожен и обязан воспроизводить эталон байт в байт.

### 📄 Документация

- **README переписан под текущее состояние.** Он всё ещё описывал бота как
  надстройку над докерным контейнером, не знал про условную маршрутизацию,
  самообновление, самопроверку старта и половину модулей. Заодно убран раздел
  «план интеграционного теста перед боевым запуском» — бот в бою давно.

## v2.1.0 — на шлюз идёт только то, что в списке

> ✅ **Обновление шлюза не требуется.** Сторона ГВ не менялась, бандл пересобирать не нужно, ключи и обфускация линка прежние.
>
> 🔻 **У личных списков переворачивается смысл.** До этой версии набор описывал то, чему нужен **зарубежный** адрес, и всё остальное уходило на шлюз. Теперь наоборот: на шлюз идёт **только перечисленное**, а прочее выходит зарубежным адресом обычным путём. Строки в личных списках при этом остаются на месте и начинают означать противоположное — «этому сайту нужен российский адрес» вместо «этому нужен зарубежный». Миграции для них нет и быть не может: понять, что человек имел в виду, из самой записи нельзя. **Просмотри списки до обновления** — свои и тех, кому функция была разрешена. Если фича никому не раскатывалась, делать ничего не нужно.

### 🔄 Смена модели маршрутизации

- **Через домашний канал идёт только то, что в списке.** Прежняя модель гнала на шлюз весь трафик, кроме перечисленного, и полоса домашнего канала становилась общим потолком для всех пользователей разом. Теперь туда уходят только российские сервисы из базового списка и личных добавлений.
- **Промах списка стал громким.** Раньше он ломал **заграничный** сервис — молча, потому что связать «сайт не открывается» с маршрутизацией почти невозможно (так отваливались сервисы с собственным адресным блоком). Теперь промах ломает российский сервис, а тот жалуется вслух: «вы заходите не из России». Чинится добавлением домена в личный список.
- **Пустой список перестал быть аварией.** Он означает «на шлюз не идёт ничего», то есть ровно то же, что выключенная функция. Сторож, не дававший фиче подняться без загруженных списков, снят за ненадобностью.
- **Отказ шлюза стал мягким.** Через него идут только российские сервисы, поэтому его падение — «Озон ругается на адрес», а не «у людей пропал интернет».
- **Базовый список — российские сервисы**, из двух источников (`itdoginfo/allow-domains` и `UnRKN/ru-blocklist`, вместе 638 доменов). Списки подсетей ушли: все сопровождаемые — это CDN и хостеры, то есть заграница.
- **Пороги живости шлюза — три замера в обе стороны.** Симметрично и намеренно: каждое переключение перекладывает трафик всех включённых профилей, и на коротком провале это дороже самого провала. Решение админа выключить функцию порогов не ждёт.

### 🩹 Починено

- **Тумблер пользователя перезапускал резолвер всем.** Состав конфига dnsmasq зависел от того, у кого режим включён, поэтому каждое нажатие переписывало все ~600 директив и вызывало `systemctl restart dnsmasq`. Рестарт роняет кэш и на секунды оставляет без DNS **всех** клиентов, включая тех, кто ничего не трогал. Снаружи это «включил режим — на минуту всё отвалилось», а тому, кто в этот момент резолвил имя, — необъяснимый отказ. Теперь конфиг держится на том, кому функция **разрешена**, а это решение админа и меняется оно редко.
- **Отказ применения маршрутизации был немым.** Сюда же прилетает не поднявшийся dnsmasq, то есть «у клиентов нет DNS вообще», — а уезжало это в журнал на сервере, и бот продолжал считать функцию живой. Теперь админу приходит тревога с текстом ошибки и отбой при восстановлении.
- **SSH-гейт мог закрыться на всех.** Фильтр не трогается при пустом списке адресов хоста — но такой же охраны на пустой список **админских** адресов не было, а там желаемое состояние вырождается в одни DROP-и: доступ закрыт всем, включая того, кто пришёл бы это чинить.
- **Досев при включении отправлял заграницу домой.** Остаток прежней модели: при переключении тумблера в набор досеивались адреса из списка заграницы, который теперь означает «на шлюз».

### 🧰 Установка и обслуживание

- **Инструкция проверки шлюза говорила обратное.** Финальный шаг `routing-host-setup.sh` предлагал резолвить `gosuslugi.ru` и тут же сообщал, что домен обязан быть из зарубежного списка, а `sberbank.ru` в наборе «не окажется никогда». Исправная установка по этой инструкции признавалась сломанной.
- **Предупреждение об эфемерности правил называло цену вдвое меньше реальной.** В том же наборе лежит `MASQUERADE` клиентской подсети, поэтому после ребута без закрепления интернет пропадает у **всех** клиентов, а не только у включённых.
- **Раздел «Условная маршрутизация» в настройках** показывается, только если шлюз сконфигурирован. Открытый по старой ссылке из истории чата — объясняет, чего не хватает, вместо переключателей, которые ничего не делают.
- Тексты пользовательских экранов переписаны под новую модель; состояние шлюза переехало в общий статусный блок главного меню и видно тем, кому функция разрешена.

### 🧹 Ревизия

Сплошной проход по кодовой базе после смены модели: удалён мёртвый код, оставшийся от вырезанных фич, а комментарии и документация сведены с тем, что исполняется. Расхождений набралось на два десятка мест, включая пять, где упразднённая модель описывалась как действующая, — и все они читаются ровно в момент разбора аварии.

## v2.0.0 — свой сервер, а не контейнер из приложения

> ⚠️ **Мажорная не по числу правок, а по смене хозяина.** До этой версии сервером фактически владело приложение Amnezia: оно создавало контейнер, вело собственный реестр имён, могло заводить пиров и управлять хостом по SSH. Бот жил поверх и подстраивался. Теперь наоборот — сервер наш, а приложение осталось только у клиентов, как удобный клиент для `vpn://`.
>
> ℹ️ Включает работу, помеченную ниже как v1.3.0 (условная маршрутизация): отдельным релизом она не выходила.
>
> 🔻 **Необратимое при обновлении:** сохранённая в БД ссылка полного доступа к серверу (root по SSH) **стирается миграцией**, а колонка удаляется. Фичи, которая её использовала, больше нет, а секрет продолжал бы ездить в каждом бэкапе, уходящем через Telegram. Нужна — достань ДО обновления.

### ⚡ Что поменялось принципиально

- **Сервер работает на хосте, а не в контейнере.** AmneziaWG поднимается нативно, docker для работы бота больше не нужен вовсе. Оба режима поддерживаются, выбор — ключом `docker.runtime` в конфиге.
- **Пиров заводит только бот.** Запись в конфиге сервера, которой нет в базе, больше не усыновляется молча: пир уходит в карантин («Устройства без профиля»), а админу приходит тревога — со звуком и мимо тихих часов. Это событие безопасности: создавать пиров, кроме бота, больше некому.
- **Первое устройство админа заводит бот сам.** Прямое следствие карантина: заведённый руками пир уехал бы в него, и администратор остался бы без доступа к собственному боту. Ровно один раз, ссылка приходит отдельным сообщением с кнопкой «Скрыть».
- **Серверная сторона приложения снята целиком.** Ушли: реестр имён `clientsTable`, реставрация устройств присланной `vpn://`, устройство полного доступа с хранением root-ссылки. Имя устройства теперь живёт ровно в одной базе — нашей.
- **Формат `vpn://` заморожен навсегда.** Клиенты остаются на приложении Amnezia, выданные ссылки продолжают работать, перевыпускать ничего не нужно.

### 🔒 Безопасность

- **Root-ссылка к серверу стёрта из базы** (см. предупреждение выше).
- **Пер-пирный SSH-фильтр начал работать.** В host-режиме цепочка врезалась в FORWARD, тогда как SSH к самому хосту идёт в INPUT: набор правил выглядел безупречно и не срабатывал никогда, счётчики стояли на нулях. Теперь точка врезки зависит от режима, а прежняя снимается.
- **Приватный ключ сервера больше не светится в общем `/tmp`.** После переезда на хост прежний код ронял распакованный конфиг в общий каталог с обычной маской и предсказуемым именем.
- **Публичный ключ сервера выводится из приватного,** а не читается из файла контейнера. Файл жил своей жизнью: смена ключа без его правки разослала бы всем конфиги с чужим публичным ключом — молча, до первого переподключения.
- **Fail-closed страж SSH** переписывается ботом, если приложение или ручная правка его стёрли, и сверяется по содержимому, а не по наличию маркера.

### 🇷🇺 Условная маршрутизация

- **Сервисы со своим адресным блоком.** Часть зарубежных сервисов держит собственную сеть, а не живёт за CDN, — а списки подсетей покрывают только CDN и хостеров. Домен попадал в набор лишь через резолв — и клиент, резолвящий мимо нас (DoH, свой резолвер, кэш), уезжал на шлюз, то есть российским адресом ровно туда, где он запрещён. Симптом был характерный: «с выключенным режимом работает, с включённым нет». Теперь такие сети зашиты списком и работают независимо от DNS.
- **Источник списков замолчал — бот скажет.** Кэш переживает недоступность источника намеренно, и ровно поэтому источник может умереть навсегда, а списки застыть без единого признака.
- **Обход перехвата DNS закрыт превентивно:** NXDOMAIN на имена DoH-эндпоинтов и REJECT на DoT.

### 🧰 Установка и обновление

- **Обновление доделывает новый код, а не старый.** `awg-bot update` подменял код на диске и продолжал звать функции, разобранные из прежней версии, — правка в них не могла применить сама себя и молчала ещё одно обновление.
- **Юнит без `docker.service` в host-режиме.** Жёсткая зависимость мешала дважды: docker нельзя было удалить, а его падение остановило бы бота, который к нему не обращается.
- **Скрипты поставки исполняемые.** `harden_firewall.sh` уезжал без бита исполнения и на запуск отвечал «command not found» — в момент, когда им закрывают SSH.
- **Сторона шлюза едет одним файлом.** `routing-link-setup.sh --apply` собирает бандл со скриптом и конфигом линка внутри и печатает готовую строку `scp`; `awg-bot gw-bundle` пересобирает его позже, не меняя ключей.
- **`install/awg-host-migrate.sh`** — перенос сервера с контейнера на хост с проверкой по факту и откатом.

### 🩹 Починено

- **Удаление профиля отчитывалось об успехе, не сняв пиры.** Сервер не ответил — профиль исчезал из базы, а доступ оставался живым.
- **Сверка пиров вставала колом** при переиспользовании адреса: держатель адреса не удалялся никогда, вместе с ним ломался подхват всех остальных.
- **Битый YAML молча съедал каждое сохранение настройки** — дубликат ключа в конфиге, и запись падала в лог, а пользователю показывалось «сохранено».
- **Гонка между тиком живости и реконсиляцией** могла на мгновение отправить на шлюз весь трафик включённых пользователей.
- **Экран настроек больше не блокирует event loop.**
- Плюс серия отказов переезда на хост: закрытый порт клиентов, умерший DNS, обратный маршрут в исчезнувшую docker-сеть, потеря обвяза шлюза после ребута.

## v1.3.0 — российский IP для российских сервисов

> ⚠️ Требует настройки на сервере. Сама по себе функция не включится: нужен шлюз с российским адресом и обвяз, который ставится скриптами из `install/`. Пока функция не настроена, она спит и на работу бота никак не влияет — **обновление безопасно накатывать и не включая её**.
>
> Обновление **не трогает существующий** `/etc/awg-bot/conf/app.yaml` (досев работает на уровне файлов, а не ключей), поэтому секции `routing:` там не появится. Без неё бот работает как раньше. Чтобы включить функцию — скопировать секцию из шаблона `/opt/awg-bot/conf/app.yaml` в боевой конфиг, заполнить `gw_interface` и перезапустить бота. Параметр холодный: он же и аварийный рубильник — очистил, перезапустил, функция выключена у всех разом.

### 🇷🇺 Новое

- **Российский IP для российских сервисов.** Банки, госуслуги и маркетплейсы перестают ругаться на зарубежный адрес: трафик к ним уходит через домашний шлюз и выходит с российского IP, а всё остальное продолжает работать как раньше.
- **Ссылки и QR-коды не меняются.** Режим — серверная настройка, а не отдельные учётные данные. Ничего перевыпускать и переподключать не нужно, тумблер можно щёлкать в любой момент туда и обратно.
- **Разрешение выдаёт админ, включает пользователь.** Разрешения раздаются в «⚙️ Настройки → Условная маршрутизация». Пока разрешение не выдано, пользователь функции не видит вовсе. Включает её он сам — или админ за него, зайдя в его профиль. Отзыв разрешения гасит эффект, но выбор пользователя сохраняет: вернул разрешение, и всё как было.
- **Сервисы, которые сами не пускают россиян, работают сразу.** Такие сервисы Россия не блокирует — значит их нет ни в одном скачиваемом списке, и при включении режима они отваливались первыми: домен уходил через российский адрес, то есть ровно через тот, который ему закрыт. Теперь бот тянет сопровождаемый список таких сервисов (категория geoblock того же источника, что и списки блокировок) и досеивает его в момент включения режима.
- **Свой список исключений.** По умолчанию через заграницу ходит только то, что в списках заблокированного, — всё остальное выходит с российского адреса. Если какому-то сайту заграница всё же нужна, пользователь добавляет его сам в разделе «Доступ к РФ-сервисам». Можно вставлять списком и целыми ссылками — лишнее бот отрежет и объяснит, что не принял и почему.
- **Один тумблер на профиль.** Режим включается сразу на всех устройствах пользователя. Пер-девайсного переключателя нет намеренно: выбор по одному устройству держал бы состояние, которое почти всегда «все» или «никто», ради третьего слоя флага в БД и на трёх экранах.
- **Друг пользуется, но не управляет.** Переданное устройство работает в том режиме, который выбрал владелец. Список адресов другу не виден.

### 🛡 Надёжность

- **Шлюз недоступен — интернет не пропадает.** Бот замечает это сам и снимает маршрутизацию: российские сайты временно открываются с зарубежного адреса, но связь работает. Уведомление приходит только админу — один раз на смену состояния.
- **Аварийное снятие режима наконец работает.** Рефакторинг удалил константу, на которую ссылались обе ветки рубильника: попытка снять маркировку падала `NameError` и оседала в логе, то есть при отказе шлюза бот физически не мог отключить режим и трафик продолжал уходить в нерабочий тоннель. Это и был симптом «интернет вообще отваливается».
- **Живость шлюза меряется проходимостью, а не состоянием туннеля.** Бот пингует внешний адрес через шлюз, а не смотрит на возраст хендшейка. Разница принципиальна: у шлюза может быть поднят туннель и лежать аплинк — тогда хендшейки идут, бот считает шлюз живым и продолжает слать в него трафик, а у пользователя интернета нет насовсем.
- **Отказ замечается за десятки секунд, а не за минуты.** Для пользователя с включённым режимом шлюз — основной путь, поэтому его отказ это «нет интернета», а не «не тот адрес». Проверка вынесена в отдельное частое задание. Гасим режим по первому плохому замеру, возвращаем — после двух хороших подряд: выключение безопасно, включение рискованно.
- **Без спама на коротких провалах.** Режим гасится сразу — это безопасно и дёшево, — а админу сообщается только когда отказ подтвердился несколькими проверками подряд. Пара «отвалился / снова в строю» в одну минуту не несёт информации, зато приучает не читать уведомления. Проверка при этом стучится в несколько внешних целей: один хост сам по себе точка отказа.
- **Уведомление называет место ремонта.** «Шлюз молчит» чинят на линке, «за шлюзом нет интернета» — на самом шлюзе. Это разные тексты, чтобы не искать не там.
- **MSS-кламп на пути к шлюзу.** Путь инкапсулирован дважды, и без подрезки крупные сегменты умирают молча: пинг и DNS проходят, а страницы не грузятся — выглядит как «интернет отвалился», хотя связь есть.
- **`awg-bot routing-doctor`.** Послойная диагностика тракта: обвяз, линк, проходимость наружу, маршрут, рубильник, кламп, наборы. Ничего не меняет, запускать можно в момент отказа — первый сбой сверху и есть место ремонта.
- **Добавленный сайт начинает работать сразу.** Бот сам резолвит адрес и досеивает его, не дожидаясь, пока у пользователя истечёт кэш DNS. Раньше это выглядело как «добавил, но не работает; потом само заработало».
- **Статус в главном меню.** Пользователь с включённым режимом видит строку о его состоянии рядом со статусом сервера. Тем, у кого режим нигде не включён, строка не показывается.
- **Самопроверка при запуске.** Перед тем как кого-то можно будет включить, бот проверяет, что окружение действительно работоспособно: наборы ядра создаются, маркировка соберётся, маршрут ведёт на текущий адрес контейнера, dnsmasq умеет перезапускаться. При нехватке чего-либо функция честно не поднимается и пишет причину, а не имитирует работу.

### 🧰 Для установки

- В поставку добавлены скрипты `install/routing-host-setup.sh` (обвяз сервера), `install/routing-link-setup.sh` (туннель до шлюза) и `install/routing-gw-setup.sh` (сторона шлюза). Каждый по умолчанию показывает план и ничего не меняет, умеет `--apply` и `--rollback`, повторный запуск ничего не дублирует.
- Порядок развёртывания и все решения описаны в `docs/conditional-routing.md`.

## v1.2.2 — объявления пользователям, самопроверка старта

> ℹ️ Патч поверх v1.2.1: новая рассылка объявлений, самодиагностика при запуске и мелкие правки интерфейса. Отдельных действий при установке не требуется.

### 📢 Новое

- **Объявление пользователям.** Кнопка «📢 Объявление пользователям» в админ-меню: пишешь текст (можно с разметкой — <b>жирный</b>, ссылки), он уходит всем клиентам и активным друзьям, по одному разу на человека. Перед отправкой — превью и число получателей, после — отчёт о доставке.
- **Самопроверка при запуске.** Бот проверяет окружение на старте: критичные проблемы (повреждённая БД, нет места для записи, отвергнутый токен) останавливают запуск с понятным сообщением в журнале; некритичные (мало места на диске, не отвечает контейнер, недоступен IMAP аварийного выхода) приходят тебе первым сообщением после запуска.

### ✨ Интерфейс

- В списках устройств тип помечается иконкой: 🕹 — полный доступ к серверу, 📲 — устройство друга, 📱 — обычное.
- Заголовок списка устройств профиля — 📋.
