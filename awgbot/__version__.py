"""Единый источник версии бота. Бампается при каждом релизе.

Значение ДОЛЖНО совпадать с тегом соответствующего GitHub-релиза (без ведущего
«v»): тег `v1.1.0` → здесь `1.1.0`. По нему модуль обновлений находит свою
позицию в списке релизов и берёт «следующую ступень». Если версии нет среди
тегов релизов (локальная/кастомная сборка) — обновления полностью выключены.
"""

__version__ = "1.2.0"
